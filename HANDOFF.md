# Hibi（ひび）引き継ぎ書

この文書は、**次の会話で作業を続けるための指示書**です。
これまでのやり取りで決めたこと、直した不具合、二度と繰り返してはいけない失敗を、
すべてここにまとめてあります。**作業を始める前に、まずこれを最後まで読むこと。**

---

## 1. このアプリは何か

ひびの記録をつける、じぶんだけの手帳です。iPhone 17 と iPad Air 4 で使います。

- 記録は5種類：**メモ／画像／チェックリスト／リンク／スケジュール**
- 画面は下タブ4つ：**Today／みつける／計画／フォルダ**
- そのほかは右上の三本線メニューから：表示設定・タグの編集・バックアップ・ヘルプ

**書いたものは端末の中だけに保存します。外へは一切送りません。**
保存先は `localStorage`（`window.storage` があればそちら）。キーの頭は `hibi-`。

---

## 2. 渡してあるもの

| もの | 中身 |
|---|---|
| `App.jsx` | アプリ本体。これ1枚ですべて動く（約5,470行） |
| `hibi.zip` | GitHub Pages で公開する一式（Vite + React + Tailwind、GitHub Actions で自動デプロイ） |

**2つの `App.jsx` の中身は必ず同じにすること。** 渡す前に `diff -q` で確かめる。

---

## 3. 作業のしかた（重要）

### 3-1. ファイルの分け方

本体は長いので、`/home/claude/build/` で**8つに分けて**編集し、最後に連結する。

```
p1 → p2 → p3 → p4 → p5 → p6 → p6b → p7   の順に連結して App.jsx を作る
cat p1.jsx p2.jsx p3.jsx p4.jsx p5.jsx p6.jsx p6b.jsx p7.jsx > App.jsx
```

| ファイル | 中身 |
|---|---|
| p1 | 保存のしくみ、日付の道具、色、記録・計画・フォルダの形（データの決まり） |
| p2 | 共通の部品（入力欄、ボタン、ドラム、カレンダー、小窓、見出し、メニュー） |
| p3 | 記録を書く画面（RecordForm）と、その部品 |
| p4 | 記録の札、日・週・月の画面、写真を大きく見る窓、まとめて選ぶしくみ |
| p5 | みつける、計画、種類、計画の中身 |
| p6 | フォルダ、表示設定、タグの編集、バックアップ、ヘルプ |
| p6b | グローバルCSS（`GLOBAL_CSS` の文字列） |
| p7 | 全体の組み立て（AppMain）、下タブ、保存の受け渡し |

### 3-2. 直したら必ず確かめる

**目で見ずに渡さないこと。** 過去に何度も、見た目の崩れや動かない不具合を見逃している。

```bash
# 1) 組み立てて、エラーが出ないか
cd /home/claude/build && cat p1.jsx p2.jsx p3.jsx p4.jsx p5.jsx p6.jsx p6b.jsx p7.jsx > /tmp/try.jsx
# 2) ブラウザ用に組む（esbuild）→ 3) 画面ごとの下描き試験 → 4) 実際に触る試験
```

道具は `/tmp/rtcheck2/` に置いてある（次の会話では作り直しが必要）。

- `test.jsx` … 画面を1つずつ描いてみる試験（約50件）。**記録が空の状態も必ず試す**
- `drive2.cjs` … 通しの操作（記録を書く→並ぶ→チェック→日週月→検索→計画→フォルダ→読み直し）
- `plan2.cjs` `month.cjs` `cond.cjs` `sel.cjs` `rep.cjs` `drum.cjs` `menu.cjs` `cancel.cjs` `audit.cjs` … それぞれの機能
- `look2.cjs` `form2.cjs` `misc.cjs` … 見た目を画像にして目で見る

**注意：ネットにはつながらない。** npm install はできない。react / react-dom / playwright / sharp /
esbuild は `/home/claude/.npm-global/` にすでにある。Tailwind は入れられないので、
見た目を確かめるときは `minitw.cjs`（使っているクラス名から簡易CSSを作る自作の道具）を使う。

試験の画面には **`<meta name="viewport" content="width=device-width, initial-scale=1">` を必ず入れる。**
入れ忘れると横幅が980pxになり、押せない・ずれるという嘘の結果が出る。

---

## 4. 見た目の決まり

添付画像（ペールトーンの色見本、Investing Tracker UI Kit、Itinerary）に寄せてある。

- **地は白**。札は白＋ごく薄い影（`.card-soft` = `0 1px 3px rgba(30,60,55,.07)`）＋1pxのうすい線
- 画面いちばん上は**基調色の帯**、題は中央、左右にボタン（48px四方）
- 色は**ペールトーン**。基調 `#57B79B`。記録の8色も淡い色（`COLORS`）
- **濃い色・太い線・重い影を使わないこと。** 1つでもあると、そこだけ浮いて見える
- **面を色で塗りつぶさないこと。** 色は「いま選んでいる所」「大事な数字」など要所だけ。
  選んだタブは白い札にして、色ではなく浮き沈みで示す
- 見出しの帯だけ、ごく淡い流れ（`.bg-head`）をつけてある。**強い流れにしないこと**
- 札は**枠なし・影だけ**。枠と影を両方つけると、そこだけ厚ぼったく見える
- 字は Noto Sans JP。**太さは600まで**（`font-bold` も600に読み替えている）
- 線の太さ：枠は1px、しるし（アイコン）は1.75、チェックの印だけ3.5（`className="thick"` を付けた要素だけ）
- 角丸：札は16px（`rounded-2xl`）、ボタンは12px（`rounded-xl`）
- ボタンの高さ：ふつう **50px**（`.btn-h`）、いちばん大事なものは **54px**（`.btn-h-lg`）。
  **これより低いボタンを作らないこと。** 押しづらいと何度も指摘されている
- 絵文字は**使わない**

### 文字の大きさ（小・中・大）

`GLOBAL_CSS` の中に、使っている `text-[Npx]` すべてぶんの上書きがある（中＝1.09倍、大＝1.22倍）。
**手で書き足さないこと。** 抜けや逆転（中より大が小さい）が起きる。
新しい大きさを使ったら、次の方法で作り直す：

```python
# 使われている text-[Npx] を全部拾って、.ft-font-m / .ft-font-l の一覧を作り直す
```

---

## 5. 触り心地の決まり

演出は `ft-*` として `GLOBAL_CSS` に1か所へ集めてある。
**ボタンごとに `active:scale-…` を書かないこと。** 深さや速さがばらついて、全体の手ざわりが崩れる。

- `ft-tap` `ft-tap-card` `ft-tap-icon` … 押したときの縮み
- `ft-seq` … 順に浮かび上がる
- `ft-sheet-wrap` / `ft-sheet-box` / `ft-sheet-body` … 下から出る小窓は必ずこの3点セット
- `animation-fill-mode` は必ず `backwards`

### 重ねる高さ（zIndex）

**小窓の上でさらに小窓を開くときは、必ず高くすること。** 同じ高さだと下に潜って、
見えているのに押せなくなる。これで2回つまずいた（フォルダのタグ選び、繰り返しの日付選び）。
`TagPickDialog` と `DateInput` は `zIndex` を受け取れるようにしてある（既定 2147483000、上に重ねるときは 2147483250）。

---

## 6. これまでに直した不具合（同じ失敗を繰り返さないため）

| 症状 | 原因 | 直し方 |
|---|---|---|
| 繰り返しのチェックを押すたび、同じ札が増える | その日ぶんの実体を毎回あたらしく作っていた | 実体に `fromRepeat`（もとのid）を持たせ、**すでにあればそれを使う**。仮の札も出さない |
| 繰り返しの予定が、次の日に出てこない | 繰り返しの判定をチェックリストにしか掛けていなかった | 予定にも掛ける（日・週・月すべて） |
| 繰り返しの小窓を開くと画面が真っ白 | `RepeatEditor` を別の作業のついでに消していた | 部品を消すときは、呼び出し元を必ず探す |
| ドラムを押しても選べない | 外枠が指の受け取りを捕まえ、行のタップが届かない | 枠の `onPointerUp` で、押した位置から行を計算して転がす |
| ドラムで1つ下が選ばれる | 位置の計算で行の高さの半分を引き忘れ | `- WHEEL_ITEM_H / 2` を必ず入れる |
| 縦長の写真がタイムラインを埋める | クラス名（`aspect-[4/3]`）だけに頼っていた | `style={{ aspectRatio: "4 / 3" }}` で直に指定 |
| 左上の戻るボタンが押せない | 左端の「払って戻る」透明な帯が上に重なっていた | 帯は `top-16` から下だけにする |
| 保存したのに「書きかけ」が残る | 画面を閉じたあとも自動保存の仕掛けが動いていた | 入れた覚え（listener）は必ず外す。保存後は `doneRef` で止める |
| 同じ本文が二度出る | 見出しに1行目を使い、本文でも全文を出していた | 本文は2行目から（`restOfLines`） |
| 種類だけ選んでも記録が集まらない | タグが空だと何も集めない作りだった | タグ・種類は**どちらか片方だけでも**集める |
| 三本線を押したあと色が残る | `hover` の色が触ったあとも消えない | 背景色を使わず、縮む動きだけにする |
| 画面が二重に残る | 重なる画面のうち**ひとつだけ**に `key` を付けていた | 覆いに `key` を付けない |
| 中より大の文字が小さい | 大きさの一覧が手書きで抜け・逆転 | 使っている大きさから自動で作る |

---

## 7. 決めごと（作りの方針）

- **タイムライン**：記録はたたまない。中身ははじめから全部見える
- **左の細い帯**：時刻のある記録だけ、点線と小さな点でつながる（串）。
  **ここにしるし（種類の絵）を置かないこと。** 札が細くなって読みにくい。しるしは札の中の1行目
- **ピンと鉛筆**：まるい皿にのせて隣り合わせ。ピンは「上向きの矢印」
- **コメント**：吹き出し。中は白のまま、細いグレー（#B9BDC2・1.4px）の線で囲う。
  しっぽはSVGで描き、**同じ絵の中に白い帯を入れて**本体の上の線を消す
  （別の要素で消そうとすると、位置がずれて線が見えてしまう）。
  しっぽの2辺は、本体の線より下へ伸ばさない（足が生えたように見える）。
  **中を色で塗らないこと**（札の上でうるさく見える）
- **見出しの帯**：好きな写真を敷ける（表示設定 →「見出しの写真」）。
  写真の上には必ずうすい黒をかぶせてから字をのせる（明るい写真だと読めない）
- **入力画面**：項目名も説明も出さない。欄の中の薄い字だけで伝える。並びは
  「いつのこと → タグ → 中身 → コメント」。**画面ごとに順番を変えないこと**
- **画面の役割と置き場所をそろえる。**
  右上＝その画面の「設定」（名前を変える・消す）、右下＝その画面の「足す」、
  記録の右上＝「選ぶ」。**画面ごとに入れ替えないこと**
- **「選択」は、それが効く記録の**すぐ上・**いつも右はし**（見出しの帯には置かない）。
  ボタンは**どの画面でも同じ、まるいしるしのボタン**（44px・チェックのしるし／選ぶ最中は色つきで×）
- **チェックの丸は、どこでも同じ形・同じ大きさ**（まるい枠2px、記録の中は20〜24px）
- **右下のボタンの絵は、画面ごとに変える。** Today／みつける＝＋、計画＝的、
  フォルダ一覧＝フォルダ＋、**フォルダの中＝ふるい**（どの記録を集めるかを決める場所だから）。
  ひとつ上の画面と同じ絵にすると、どちらにいるのか分からなくなる
- **ピン留めは押しピンの絵**（`Pin`）。留めているときは中を塗る。
  **上向きの矢印にしないこと**（「上へ動かす」と読めてしまう）
- **えらぶ最中は、札ぜんたいを押せるようにする。** 枠を破線にして「押せる」と伝え、
  丸（28px）は**札の外（左）**に出す（中に置くと種類のしるしと見分けがつかない）。えらんだ札は色の枠＋うすい色で塗る。
  **小さな丸だけを的にしないこと。** どこを押せばよいか分からない
- **タグを追加ボタンは、付けたタグの上**（下に置くとタグが増えるたび逃げる）
- **チェックリスト**：長い字は折り返す（1行の入力欄にしない）。
  並べ替えは**右の三本線をつまんで運ぶ**。**小さな上下の矢印にしないこと**（押し分けにくい）
- **週の画面**：7日ぶんを2列で並べる（1列だと縦に長くなり、一週間を見わたせない）
- **月のマス**：予定の字は9pxでも、全角5文字ぶんは入るようにする
- **計画**：**種類がひとつでも、作った計画を勝手にそこへ入れないこと。**
  まず「種類なし」で独立して並び、設定からあとで種類へ移す。
  計画にもピン留めがあり、並びは「固定 → ふつう → やり遂げたもの」。
  「この計画をやり遂げた」を押すと日付が残り、**ひと呼吸のお祝い**（`.ft-celebrate`）が出る。
  やることは2段のチェックリスト。上の段の字は左はし、中の項目はひと目盛内側。
  期限の近い順に並び、済んだものは下へ
- **フォルダ**：入口は右下のボタン1つ（記録を手で選ぶ／タグや種類で集める／名前を変更／削除）。
  条件は「保存」で決める。手で外した記録は `excluded` に入り、条件に合っても入らない（戻す道あり）
- **月の画面**：マスには予定だけ（最大4件）。チェックリストは出さない。
  はじめは今日がえらばれていて、下にその日の予定とチェックリストが出る
- **時刻は必ず2けた**（09:30）。先頭の0を取らない
- **選ぶ・決める欄を、出したり消したりしないこと。** 場所が動くと、押そうとした先が消える。
  効かないときは、うすくして押せなくしておく（例：繰り返しの「いつまで」）

### 消した機能（指示による。**勝手に戻さないこと**）

- リスト（日付に縛られない控え）
- 毎日やること（習慣）
- まとめて登録（複数の日にチェックリストを作る）
- 記録の種類の呼び名を変える設定
- 動画のリンク（画像だけにした）

---

## 7-2. 写真の置き場（対応ずみ）

**写真は記録の中に持たず、別の置き場（IndexedDB）に入れてある。**
記録が持つのは「photo:番号」という目印だけ。

- 保存の前に `stashPhotos(rec)` を必ず通すこと。**飛ばすと記録の中に絵が入り、すぐ保存できなくなる**
- 画面に出すときは `<Photo src={…} />` を使う。**img の src に「photo:…」を直接渡さないこと**
- 記録を消したら `sweepPhotos(records)` で、使われなくなった絵を片づける
- 置き場が使えない端末では、これまでどおり記録の中に持つ（自動で切り替わる）

実測：写真1枚あたり記録は **308文字**。100枚でも記録は 30KB ほど。
写真の実体は1枚 **127KB**（長辺900px・webp0.72）。置ける枚数は端末の空き容量しだいなので、
**決め打ちで書かないこと。** バックアップの画面に、`navigator.storage.estimate()` で
「置ける見込み・いまの使用量・あと何枚ほど入るか」を出してある。
起動時に `askPersist()` で「消さないでほしい」と端末にお願いしている
（iPhone は、ホーム画面に追加していないと、しばらく使わないだけで消されることがある）。
（前は1枚で174KB、30枚ほどで上限に当たっていた）

写真1枚の重さ（長辺900px・q0.72）は **127KB** ほど。置き場の上限は端末によるが、
ふつうは数百MB〜。毎日10枚でも当分もつ。

### バックアップ

書き出す前に、2つのつまみで決める。

- **写真もふくめる** … 入れると重いが、これだけで元どおりに戻せる。切ると軽い（戻しても写真は出ない）
- **合言葉でロックする** … ファイルの中身を読めなくする（AES-GCM＋PBKDF2 15万回、端末の Web Crypto）。
  **合言葉を忘れると二度と開けない。** 画面でも必ずそう伝えること。
  ロックしたファイルは `{app:"hibi-locked", salt, iv, body}` だけの形になり、開くと文字の羅列に見える

出し方は3つ。**ひとつしか道を用意しないこと**（端末によって使えるものが違う）。

1. **保存先を選んで書き出す** … `showSaveFilePicker`（Android の Chrome など）。場所を自分でえらべる
2. **送る・保存する** … 共有シート（iPhone の「ファイルに保存」ほか）。だめならダウンロード
3. **文字でコピーする** … 最後の逃げ道（ロック時は使えない）

形式は `.json` で、iPhone でも Android でも扱える。
読み込みでは、`app === "hibi-locked"` なら合言葉を聞いてから開く。`obj.photos` があれば置き場へ書き戻す。

## 8. まだ手をつけていないこと・気になっていること

- **iPhone実機での確認はできていない。** ブラウザ（Chromium・幅393px）での確認のみ
- 写真の置き場は IndexedDB へ移しずみ（7-2）。**古い版から移ってきた記録**（記録の中に絵を持ったまま）は、
  そのままでも表示できるが、置き場へ移す片づけはまだ作っていない
- バックアップの読み込みは、書き出したファイルを貼り付ける形。ファイル選択には未対応
- 週の画面は日付ごとの一覧のみ。時間帯の帯（ガントのような見せ方）は未対応

---

## 9. 次に作業する人へ

1. `App.jsx` を `/home/claude/build/` で8つに分け直すか、`App.jsx` を直接編集する
2. 直したら**必ず**：組み立て → 下描き試験 → 実際に触る試験 → 画像で目視
3. 渡すときは `App.jsx` と `hibi.zip` の両方、中身が同じことを `diff -q` で確認
4. 説明は**専門用語を使わず**、何をどう直したか・何を確かめたかを短く書く

**「たぶん動く」で渡さないこと。** 動かして、見て、それから渡す。

### 押せるものの大きさを、まとめて調べる道具

`sweep.cjs` は、画面ごとに「高さ40px未満のボタン」「画面からはみ出しているもの」を洗い出す。
**大きさをクラス名（min-h-[…]）だけで決めた所は、当て方によっては効かない。**
効かなかった例：日・週・月の切り替えが18pxになっていた（`style={{ minHeight: 44 }}` に直した）。
