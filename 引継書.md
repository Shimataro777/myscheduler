# 引継書：My手帳 実装ガイド

対象読者：これから `my-techo-pages`（app.js / app.css）に新しい画面・部品を追加する人（未来の自分を含む）。

この文書は「今すぐ使える早見表」です。二部構成になっています。

- **第1部 画面をつくるとき** … 文字・余白・色・部品の決まり。新しい画面を足すたびに開く。
- **第2部 保存とタップ** … チェックの「先祖返り」を三度くり返して学んだこと。保存や印の入り切りに触るときは必ず読む。

> 🧭 **新しい画面・シート・小窓を足すときは、先に第1部3章「画面遷移の決まり」を開くこと。**
> どの向きから出すかは、画面ごとの好みではなく「どういう移り方か」で決まっています（2.11.0〜）。
>
> 🔝 **下タブ（Today／探す／計画／フォルダ …）に画面を足すときは、第1部3章「下タブをもう一度押したときの戻り方」も開くこと。**
> どのタブも「1回め＝いま見ている表示のいちばん上」→「2回め＝デフォルトの表示」の共通ルールです（2.11.13〜）。

経緯や調査の詳細は `UIデザイン仕様書_ドラフト.md` にまとめてあるので、「なぜこうなっているか」を知りたいときはそちらも参照してください。

---
---

# 第1部　画面をつくるとき

## 1. まず知っておくこと：ファイルの構成

- **app.css**：静的なユーティリティCSS。`:root` にデザイントークンが入っている。
- **app.js**：アプリ本体（React、JSXはトランスパイル済みの `createElement` 形式）。ファイルの末尾近くに `GLOBAL_CSS` というテンプレート文字列があり、ここにも追加のCSSが入っている（`app.css` より**あとから**読み込まれる）。
- **index.html**：`app.css` と `app.js` を `?v=バージョン番号` 付きで読み込む。

> ⚠️ **app.css と app.js 内のCSSは、同じクラス名を定義すると後者（app.js）が勝ちます。**
> 新しいクラスを探すときは両方 `grep` すること。片方だけ見て「値がこうなっているはず」と思い込むと外れます（実際に `.btn-h` の高さ表記がこれで食い違っていたのを見つけて直しました）。

> ⚠️ **同じことが「部品の名前」でも起きます。** app.js はひとつの大きなモジュールなので、
> `function 名前(...)` を二度宣言すると、**あとに書いたほうが全部を乗っ取ります。**
> 実際に `TapButton` という名前で二度目を足してしまい、「戻る」と「キャンセル」が
> 押しても何も起きない状態を作りました（下の4章を参照）。
> **新しい部品を足す前に、その名前で `grep -n "function 名前"` すること。**

---

## 2. 新しいテキストを置くとき：`.fs-*` を使う

生の `text-[13px]` のようなクラスは**作らないこと**。次の8段階＋数値専用2段階から選びます。

| クラス | サイズ(小) | 用途の目安 |
|---|---|---|
| `.fs-micro` | 9.5px | バッジ内の数字、極小ラベル |
| `.fs-caption` | 11.5px | 補助テキスト・注釈・ヒント |
| `.fs-label` | 12.5px | 日付・件数などの二次情報ラベル |
| `.fs-body-sm` | 13.5px | カード内本文、リスト項目、副次的な説明文 |
| `.fs-body` | 14.5px | 標準本文、ボタン文字 |
| `.fs-subhead` | 15.5px | セクション見出し、計画名、強調したいラベル |
| `.fs-title` | 17.5px | 画面タイトル、モーダル見出し |
| `.fs-display` | 21px | エラー画面などの大見出し |
| `.fs-displaylg` | 22px | 月見出し、お祝い見出しなど、見出しより一段大きい特別な文字 |
| `.fs-stat` | 20px | 達成率など「見て数える」数字専用 |

**太さは含まれていません。** `.fs-subhead` だけでは太くなりません。太字にしたいときは今まで通り `font-bold` を一緒に付けてください（実装中に「見出しだけ勝手に太くなる」事故を起こして学んだ教訓です）。

### 文字サイズ設定（小・中・大）と連動させる、絶対に踏んではいけない地雷

`.fs-*` は `--ft-scale` という変数を使って自動的に拡大縮小します。新しく似たようなクラスを作るときは、必ずこの形にしてください。

```css
/* 正しい書き方 */
:root { --fs-example: 14px; }                                         /* ← 素の値だけ。倍率を混ぜない */
.fs-example { font-size: calc(var(--fs-example) * var(--ft-scale)); }  /* ← 掛け算はここで */

/* やってはいけない書き方（一見動くが、設定を変えても反映されない） */
:root { --fs-example: calc(14px * var(--ft-scale)); }                  /* ← :root で確定してしまう */
.fs-example { font-size: var(--fs-example); }
```

CSSのカスタムプロパティは「宣言した要素」の上で中身が確定する仕様のため、`:root` の中で `calc(… * var(--ft-scale))` と書くと、その場で `--ft-scale:1` が代入されて固定値になります。これは実際に一度やってしまい、全画面で文字サイズ設定が効かなくなる不具合を起こしました。**新しいトークンを追加したら、小・中・大の3段階を実際に切り替えて確認すること。**

### 入力欄（input / textarea）だけは特別

自分で打ち込む文字（メモ本文・タイトルなど）は `.ft-input` を使います。これは **16px を下回らない**ようにしてあります（iPhoneのSafariが16px未満の入力欄をタップすると画面を勝手に拡大してしまうため）。新しい入力欄を作るときも `font-size: max(16px, calc(基準px * var(--ft-scale)))` の形を守ってください。

---

## 3. 余白・アイコン・色・モーションの早見表

### 余白（4px刻み＋半段階）

```
2px  4px  6px  8px  10px  12px  16px  20px  24px  28px  32px
```

この並びの値だけを使う。中途半端な値（`13px` `18px` など）は使わない。

### 記録カードの中の余白（2.11.8〜）　⚠️ カードの種類ごとに変えないこと

リスト・メモ・スケジュール・イベントなど、**記録を表す白いカードは、すべて同じ内側の余白にそろえる。**

| 場所 | クラス | 値 |
|---|---|---|
| カードの内側（上下） | `py-3.5` | 14px |
| カードの内側（左右） | `px-4` | 16px |
| 見出し行（しるし・時刻や期限の札・固定／編集）の下 | `mb-0.5` | 2px |
| 右上の固定・編集ボタンのかたまり | `-mr-1.5 -mt-1.5` | 皿（`w-8 h-8`）の余白ぶん外へ寄せ、見た目の上辺・右辺をカードの余白にそろえる |
| チェック行の並び | `-ml-1.5 space-y-[2px]` | 行の当たり（`px-1.5`）ぶん左へ戻し、丸の左端をタイトルの左端にそろえる |
| メモ（本文の引用） | `mt-2 pl-1.5 border-l-2` | 8px |

- 14px は上の刻みに無い値だが、`RecordRow` の `<article>` が最初からこの値で、ほかのカードもこれに合わせてある。**記録カードの内側に限って使ってよい。** ほかの場所には広げないこと。
- **余白は外の箱ひとつで持つ。** 段ごと（見出し・チェック行・メモ）に `pt-*` / `pb-*` / `mx-*` を分けて持たせると、有る段・無い段の組み合わせで上下の余白がばらつく。今の基準は `RecordRow`（リスト記録カード）。イベント（`StepCard`）もこの形にそろえてある。
- **使うクラスが app.css にあるか、かならず確かめる。** app.css は必要なクラスだけを入れた静的なCSSで、Tailwind の全クラスが揃っているわけではない。無いクラスを書いてもエラーにならず、**黙って余白が 0 になる。**
  - 実際に 2.11.7 でイベントのカードに `pt-3.5` と書き、上辺とアイコンがくっつく不具合を出した（`py-3.5` はあるが `pt-3.5` は無い）。
  - 確かめ方：`grep -F '.pt-3\.5' app.css`（`.` は `\.` と書かれている）。無ければ、ある値（`py-3.5` など）で書き直すか、app.css に足す。

### アイコン（lucide-react の `size`）

コンテナの大きさに合わせて選ぶ。新しい組み合わせが出てきたら、ここに追記すること。

| コンテナ | アイコンサイズ | 例 |
|---|---|---|
| `w-6 h-6` | 14px | タグ削除 |
| `w-8 h-8` | 16px | フィールドクリア |
| `w-9 h-9` | 17px | 行内の削除 |
| `w-11 h-11` | 20px | シート内の閉じる |
| `min-w-[44px] min-h-[46px]` / `w-12 h-12` | 24px | ヘッダーの閉じる（標準） |
| `min-w-[52px] min-h-[46px]` | 28px | ヘッダーの閉じる／キャンセル（広め） |
| `typeIcon()` 全般 | 22px | 記録の種類アイコン |

### 色の3つの役割（すでにコードで実装済み。混ぜないこと）

1. **記録固有の色**：`COLORS` 配列（8色）から `useTypeColor` / `useSchedColor` で取り出し、`style={{ background: color.deep }}` のように直接あてる。CSSクラスにしない。
2. **アプリの基調カラー**：`THEMES` → `--th-*` 変数。保存ボタンなど、記録の種類に関係なく常に同じ色にしたいシステム系UIに使う（例：`BTN_PRIMARY = "bg-th-950 text-white"`）。
3. **状態色（危険・警告）**：`--color-danger-*` / `--color-warning-*`。破壊的操作やお知らせバナーに使う。この2系統以外の新しい状態色（成功・情報など）が必要になったら、同じ命名規則（`--color-役割` / `--color-役割-surface` / `--color-役割-text` など）でここに追加する。

新しい部品を作るとき、「これはどの記録にも共通の操作か（→2）」「特定の記録・予定に結びついた見た目か（→1）」「注意を引きたい状態か（→3）」を最初に判断してください。

### モーション

| トークン | 値 | 使うところ |
|---|---|---|
| `--ease-out` | `cubic-bezier(0.22,1,0.36,1)` | 開く・伸びる（一番よく使う） |
| `--ease-spring` | `cubic-bezier(0.34,1.3,0.5,1)` | ポンと弾む（ピン留め・チェック・チップ出現など） |
| `--ease-settle` | `cubic-bezier(0.16,1,0.3,1)` | 静かに収まる（トグルスイッチ、ドラッグ並べ替え） |
| `--ease-in` | `cubic-bezier(0.55,0,0.68,0.53)` | 閉じる・消える |

新しいアニメーションを作る前に、まず既存のキーフレーム（`app.js` 内 `GLOBAL_CSS` で `@keyframes ft-` を検索）で表現できないか確認すること。本当に新しい動きが要るときだけ、この4種類のイージングの組み合わせで作る。

### 画面遷移の決まり（2.11.0〜）　🧭 新しい画面を足すたびに見ること

画面が**入ってくる向き**は、「どういう移り方か」だけで決める。画面ごとに見た目の好みで選ばないこと。

| 移り方 | 入ってくる向き | 使うもの | 長さ | 今ある例 |
|---|---|---|---|---|
| **階層が深くなる**（一覧→詳細、カード→中身、メニュー→設定など） | **右から** | `OverlayScreen from="right"`（中で `anim-right`） | `--ft-enter-push` 300ms | `PlanDashboard` / `FolderDetail` / `DayScreen` / `SettingsScreen` / `TagManageScreen` / `BackupScreen` / `HelpScreen` |
| **一時的な入力・独立した作業の全画面**（モーダル的なもの） | **下から** | `OverlayScreen from="bottom"`（中で `anim-up`） | `--ft-enter-modal` 340ms | `RecordForm` / `StepForm` |
| **下から出る紙**（選ぶ・短く決める） | **下から** | 暗がりに `anim-fade`、紙に `anim-sheet` | `--ft-enter-sheet` 300ms | `WheelSheet` / `TypePickSheet` / `SheetDialog` / `MoveItemSheet` / `CropSheet` など |
| **暗がり・中央や上に出る小窓・写真の拡大・お祝い** | その場で薄く出る | 外わくに `anim-fade`（中身の `anim-pop` は**空**） | `--ft-enter-fade` 200ms | `ConfirmDialog` / `NameDialog` / `DraftCard` / `PhotoViewer` |
| **メニュー（右の板）** | 右から | `SideMenu`（指について動く。独自の transition） | 280ms | ヘッダー右の ≡ |
| **同じ階層の切り替え**（下タブ、日・週・月の帯そのものの切り替え） | **動かさない** | ― | ― | `ft-tabswap` は空のまま |
| **前後の日・週・月送り**（画面上部の矢印・左右に払う） | **横に揺れる**（Today へ戻るときと同じ動き） | `ft-page-l` / `ft-page-r` | 320ms | `TodayScreen` の日付帯（2.11.10〜。詳しくは「下タブ『Today』を押したときの戻り方」の節） |

**迷ったときの見分け方**

- 「戻る（‹）」で帰る画面 → 右から。左端から払って戻れるようにする（`useEdgeSwipeBack`）。
- 「キャンセル」「✕」「保存」で閉じる画面 → 下から。左端の払いは付けない。
- 全画面ではなく、うしろの画面が上に見えている → シート（下から）か小窓（その場で薄く）。
- タブや日付を切り替えるだけ → 動かさない（例外は下タブ「Today」で今日へ戻るときだけ。下の節）。

**やってはいけないこと**

- ❌ **左から・上から入る向きを足さない。** 「戻る」の向きと混ざり、どこへ行ったか分からなくなる。
- ✅ **退場は、登場と対にすること（2.17.3〜。全画面だけは 2.17.0〜）。** 右から入った画面は右へ（`anim-right-out`）、下から入った全画面は下へ（`anim-down-out`）、下から出た紙は下へ（`anim-sheet-out`）、その場で薄く出た小窓はその場で薄れる（`anim-fade-out`）。暗がりも同じ長さで薄れる（`anim-scrim-out-push` / `-modal`）。
  - 長さは `useClosing` の待ち時間と**必ず同じ**にすること。`FT_EXIT_RIGHT_MS`（300）＝`--ft-enter-push` ／ `FT_EXIT_BOTTOM_MS`（340）＝`--ft-enter-modal` ／ `FT_EXIT_SHEET_MS`（300）＝`--ft-enter-sheet` ／ `FT_EXIT_FADE_MS`（200）＝`--ft-enter-fade`。**片方だけ変えないこと**（消えたあと待つか、消える前に閉じるかのどちらかになる）。
  - 下寄せ（`items-end`）の紙は、外わく（暗がり）の薄れも紙が下りきるまで（`--ft-enter-sheet`）続けること。短いままだと、紙が下りきる前に暗がりごと消える。
- ❌ **✕・キャンセル・背景タップだけを動かさない。** 「保存」「決定」「選ぶ」も同じ道（`requestClose`）を通すこと。片方だけ動かすと、押すボタンによって消え方が変わり、ちぐはぐに見える（下の「閉じるときの待ち時間」）。
- ❌ **紙（`anim-sheet-out`）の退場を `@keyframes` で書かない。** キーボードが出ているあいだ、紙の箱は `installKeyboardInset` が `transform` で持ち上げている。`translate3d(0,0,0)` から始まる keyframes は動きが `transform` を奪うので、持ち上げていた高さぶん**一瞬ストンと落ちてから**滑りはじめる。`transform` + `transition` の `!important` で書き、いま居る場所から下まで、ひと続きで滑らせること。
- ❌ **画面の中の部品（アイコン・文字・札・チップ）に、出てくる動きや飾りの動きを付けない。** 動くのは「画面（紙・小窓）ぜんたい」だけ。`ft-chip` / `ft-rise` / `ft-seq` / `ft-tabpop` / `ft-check-in` / `ft-celebrate` / `ft-tip` / `anim-pop` などは**空のまま**にしておく。起動画面（index.html）のしるし・名前・版数も動かさない。
  - 残してよいのは、**押したことへの返事**（`.ft-tap` の沈み込み、トグルの切り替え）と、**指について動くもの**（払って戻る、メニューの板、写真の拡大）と、**Today へ戻る動き**と、**折りたたみの伸び縮み**（2.18.0〜。下）だけ。
  - **折りたたみの伸び縮み（2.18.0〜、持ち主の指示による例外）**：開け閉めを押したときに、箱の高さと薄さを ease-in-out で変える。「出てくる飾り」ではなく「押したことへの返事」の扱い。**折りたたみは、ぜんぶ `Collapse` を通すこと**（4章の表、3章「折りたたみ」）。これ以外の部品に動きを足す理由にはしないこと。
- ❌ **`anim-*` の animation に `forwards` / `both` を付けない。** 終わったあとも transform が残り、中の `position: fixed`（ドラムの紙・下のボタン・選択バー）がその箱を基準にしてずれる。いまは `backwards`。
- ❌ **動き終わった印（`data-ft-entered`）の仕組みを外さない。** animation を持ったままの箱は iPhone で自分の層に置かれ続け、中を縦に送ったときの慣性が変わる（`useSwipePages` で一度学んだこと）。`markEntered`（`useClosing` の直前）が `ft-enter-` で始まる動きの終わりを受けて印を付け、CSS の `:not([data-ft-entered])` で動きが外れる。
  - **クラスを外す形に書き換えないこと。** React が描き直しで className を付け直すと、動きがもう一度流れる。`data-*` は React が触らないので残る。
- ❌ **`[data-ft-overlay]` と `.ft-sheet-wrap` の `overflow: hidden` を消さない**（`.ft-sheet-wrap` は 2.16.0 から、使える端末では `overflow: clip`。切り取るのは同じで、送る余地を作らない）。 入ってくる途中の画面が画面の外へはみ出し、うしろが横や下へ送れてしまう。

**新しい全画面を足すときの手順**

1. 上の表で移り方を決める（右か下か）。
2. `OverlayScreen` に `from` を渡す。自前で `fixed inset-0` の外わくを作らない（暗がりの `data-ft-scrim`、はみ出し止め、払って戻るがそろわなくなる）。
3. 右からの画面なら `useEdgeSwipeBack` をつなぐ。
4. `AppMain` の `goFromMenu` に、その画面を閉じる行を足す（メニューから別の画面へ移るとき下に残らないように）。
5. 動きの長さを変えたいときは、画面ごとに書かず `--ft-enter-*` を変える。

**メニューから画面へ移るとき（`SideMenu` の `instant`）**

- メニューの行を押して別の画面へ移るときは、板を**その場で消す**（`SIDE_MENU_GONE`）。板を右へ滑らせると、右から入ってくる画面と逆向きにすれ違う。暗がりだけは薄れさせる（新しい画面の暗がりと入れかわり、明るさが跳ねない）。
- ❌ **`goFromMenu` のタイマーの中で `setMenuInstant(false)` に戻さない。** React 19 ではこのタイマーがメニューを閉じる描き直しより先に走り、`instant` が一度も届かなかった（2.10.4 までの実際の不具合）。`false` に戻すのは、次にメニューを開くとき（`MenuContext` の値）。
- `SideMenu` は閉じた理由を `gone` で覚えている。`instant` をそのまま見て板の見た目を決めないこと。

**「画面の動き」オフ／端末の「視差効果を減らす」**

- `.ft-still` と `prefers-reduced-motion` で、すべての登場の動きが 0.01ms になり、すぐ定位置に出る。このときも `animationend` は届くので印は付く。新しい動きもこの仕組みの内側（`.ft-root` の中）に置くこと。

### 下タブをもう一度押したときの戻り方 ── 全タブ共通のルール（2.11.13〜）　🔝 下タブの画面を足すたびに見ること

**すべての下タブは、同じ二段階で戻る。** 画面ごとに「押したら何が起きるか」を決めないこと。

| 押した回数 | 起きること |
|---|---|
| **1回め** | いま見ている表示（いまのタブ・検索の結果・別の日など）のまま、**いちばん上へ**戻すだけ |
| **2回め**（1回めと同じ表示のまま） | その画面の**デフォルトの表示**へ戻して、いちばん上へ |

- **もう上にいるとき**は、1回めからデフォルトへ戻す（上へ戻しても何も変わらず「反応しない」に見えるため）。
- **もうデフォルトの表示**なら、何回押しても上へ戻すだけ。
- **あいだに表示が変わったら**（タブを払った・検索し直した・日を送った）、また1回めから数える。行って戻って同じ表示になっても数え直す。
- ❌ **押した回数（偶数・奇数）で決めない。** 表示を変えたあとの1回めで、いきなりデフォルトへ飛ぶ。
- ❌ **並び順（`sort` / `recordOrder`）や表示設定は戻さない。** それは「表示」ではなく、選んだ人の好み。
- ほかのタブから移ってきたときは、画面ごと作り直される（`ft-page` の `key: tab`）ので、もともとデフォルトの表示で出る。

**いまの各画面の「デフォルトの表示」**

| 下タブ | 1回め | 2回め（デフォルト） | 仕組み |
|---|---|---|---|
| Today | いま見ている日・週・月の上へ | 今日の「日」の上へ（横揺れ付き） | `TodayScreen` の `resetSig`（下の節。今日へ戻る動きがあるので専用に書いてある） |
| 探す | いまの検索結果（または入力途中の欄）の上へ | 条件なし・しぼりこみ欄をひらいた状態（選択中なら止める） | `FindScreen` の `useTabReturn` |
| 計画 | いまのタブ（完了済・すべて・検索中）の上へ | 検索なし・「進行中」タブの上へ | `PlanScreen` の `useTabReturn` |
| フォルダ | いまの検索結果の上へ | 検索なしの一覧の上へ | `FolderScreen` の `useTabReturn` |

**仕組み（Today 以外）**

- `AppMain` の `tabReset`（`{ find: 回数, plan: 回数, folder: 回数 }`）を、各画面の `resetSig` に渡す。
- `BottomNav` の `onChange` で、**いま開いているタブ（`k === tab`）を押したときだけ**その回数を増やす。❌ ほかのタブから移ってきたときに増やさない（作り直した直後に1回め扱いがずれる）。Today だけは今までどおり毎回 `todayReset` を増やす。
- 画面の中では `useTabReturn(resetSig, { viewKey, atDefault, toDefault })` を呼ぶだけ。
  - `viewKey` … いま見ている表示を表す文字列（例：計画は `effectiveTab + "|" + q`）。これが変わると数え直しになる。
  - `atDefault` … いまデフォルトの表示か。
  - `toDefault` … デフォルトへ戻す関数（検索を消す・タブを戻す・選択を止める）。上へ送るのと、入力欄のキーボードを閉じるのはフック側がやる。
- 上へ戻すのは Today と同じ `smoothScrollToTop()`（指が触れたら止まる／「画面の動き」オフならすぐ）。❌ 画面ごとに `window.scrollTo` を書かない。
- 下タブのボタンは `TapOnceButton` のまま（Today の節と同じ理由）。

**新しい下タブの画面を足すときの手順**

1. その画面の「デフォルトの表示」を決めて、上の表に1行足す（例：タブがあるなら最初のタブ、検索・しぼりこみなし、選択モードなし）。
2. 画面に `resetSig = 0` を受け取らせ、`useTabReturn` を呼ぶ。`viewKey` には、タブ・検索語など「見ている表示を変えるもの」をすべて入れる。
3. `AppMain` で `resetSig: tabReset.新しいタブ名 || 0` を渡す（`onChange` 側は `k === tab` で自動的に数えるので触らなくてよい）。
4. 実機で「別のタブ・検索にして下へ送る → 1回めで上 → 2回めでデフォルト」「上にいるまま押す → すぐデフォルト」「1回めのあとタブを払う → また1回めから」を確かめる。

### 下タブ「Today」を押したときの戻り方（2.10.0〜／二段階は 2.11.1〜）

> Today も上の「全タブ共通のルール」の一つ。今日へ戻る横揺れがあるため、`TodayScreen` の中に同じ決まりで専用に書いてある。**ルールを変えるときは、Today と `useTabReturn` の両方をそろえること。**


- `AppMain` の `todayReset`（押した回数）を `TodayScreen` の `resetSig` に渡す。
- **2.11.1 から二段階。** 今日の「日」以外（別の日・週・月）を見ているとき、
  - 1回め … いま見ている画面のまま、上まで戻すだけ。そのときの画面（`span|date`）を `topArmedRef` に覚える。
  - 2回め … 覚えた画面と同じなら、今日の「日」へ戻して上まで戻す。
  - あいだに日送り・日週月の切り替えで画面が変わったら `topArmedRef` を消し、また1回めから（行って戻って同じ画面になっても数え直す）。
  - ❌ **`resetSig` の回数（偶数・奇数）で決めないこと。** 別の日へ送ったあとの1回めで、いきなり今日へ飛ぶ。
  - もう画面の上にいるときは、1回めでも今日へ戻す（上へ戻しても何も変わらず「反応しない」に見えるため）。
- もう今日の「日」を見ているとき、ほかのタブから来たときは、今までどおり上まで戻すだけ。
- **下タブのボタンは `TapOnceButton`（`useTapOnce`）で受ける。** `onClick` に戻さないこと。iOS では、続けて素早く押した2回めや、縦に送った慣性が残っているうちの1回めに `click` が配られず、「1回めが反応しない → 2回めで上へ → 3回めで今日へ」になっていた（2.11.1 の最初の版で実際に起きた）。
- 上へ戻すのは `smoothScrollToTop()`。`--ease-out` に近い曲線で、距離に応じて280〜560ms。指が触れたら止まる。「画面の動き」オフ／端末の「視差効果を減らす」ではすぐ戻る。
- 日が変わるときの動きは `.ft-today-r` / `.ft-today-l`（`GLOBAL_CSS`）。**横にずらす量は記録の面の左右の余白（20px）未満にすること。** 超えると動いているあいだ画面が横に送れてしまう。
- **2.11.9 から、この横揺れひとつに統一した。** 以前は「日」から今日へ戻るときだけ横（`ft-today-r`/`ft-today-l`）、週・月からだと縦（`ft-today-in`）と割れていたが、`ft-today-in` は廃止し、span を見ずに常に横揺れ（未来から戻るときは左から、過去から戻るときは右から）にそろえた。
- **`ft-page-l` / `ft-page-r`（画面上部の日・週・月を隣へ送る矢印・払い）も、2.11.10 から同じ横揺れを使う。** 以前はここだけ空のまま（動かない）で、しかも「日」のときしか class が当たっていなかった。いまは `TodayScreen` の送り箱の key を `span + "|" + date` にして、週・月でも日付が変わるたびに必ず作り直されるようにしてある（前は `span` だけを key にしていたため、週・月では箱が作り直されず動きが再生されなかった）。
- この動きの名前は `ft-today-` で始まり、`ft-enter-` ではないので、`markEntered` の対象にはならない（動きは短く、中身を描き直すたびに流れる作り）。**名前を `ft-enter-` に変えないこと。**

### 画面の上に留める見出しと帯（2.11.3〜）　⚠️ sticky に戻さないこと

「下へ送っていくと、見出し → 日・週・月の帯の順に透けて消え、うしろの記録が見える」を直したときの決まり。

**起きていたこと（実機の画面写しで確認）**

- 見出し（`ScreenHeader`）と帯は `position: sticky` だった。消えるとき、部品は**上へ流れず、貼りついた場所のまま中身だけが描かれなくなる**（見出しの場所に記録が透け、帯はまだ下に残っている）。
- iPhone（WebKit）は sticky の部品を、画面の上に貼りついた場所ではなく**流れの中の本来の場所**で「見えているか」を判断し、本来の場所が画面から大きく離れると絵を捨てる。本来の場所がいちばん上にある見出しから先に消え、少し下にある帯があとから消える。
- 下タブと右下の＋（`position: fixed`）は同じ画面で消えていない。
- Chromium ではこの捨て方をしないので再現しない。**Mac や Android で確かめて「直った」と判断しないこと。**
- 2.11.2 で `.pad-fab` の下の余白を直したが、原因ではなかったので 2.11.1 の形に戻した。

**いまの作り**

- 下タブの4画面（Today・探す・計画・フォルダ）は、見出しと帯を `TopChrome` で包む。中は `.ft-topchrome`（`position: fixed; top: 0`）で画面に留め、流れの中には**同じ高さの場所取り**を置く。見出し・帯はもともと画面のいちばん上から並んでいたので、見た目は sticky のときと同じ（修正前後の画面写しを、上・途中・いちばん下で見くらべた）。
- 場所取りの高さは `ResizeObserver` で追う（文字の大きさ・ヘッダーの写真で変わる）。測るのは `useLayoutEffect`。
- フォルダの「記録を入れる」シートの手動タブの「検索する」札も、送る箱の中の sticky をやめ、送る箱の外（上）に置いた。

**やってはいけないこと**

- ❌ **画面ぜんたいを送る場所で、見出し・帯を `position: sticky` にしない。** 新しい画面も `TopChrome` を使う。
- ❌ **送る箱（`overflow-y-auto`）の中で、札や帯を sticky にしない。** 送る箱の外に置く。
- ❌ **`TopChrome` の中の帯に `sticky` や `top` を付けない。** 二重に下がる。
- ❌ **場所取りの高さを数字で決め打ちにしない。** 文字を大きくすると、中身が見出しの下にもぐる。

### 左端から払って戻るときの暗がり（2.10.1〜）

- 重なる画面（`OverlayScreen`）の暗がりには `data-ft-scrim`、外わくには `data-ft-overlay` が付いている。`useEdgeSwipeBack` はこれを探して、**画面を払う量に合わせて暗がりを薄くする。**
- 以前は画面だけを払い出していたので、払い終えてから閉じ終わる（当時は `useClosing` が230ms待っていた）までのあいだ、うしろの画面が暗いまま見えていた。**この印を外さないこと。**
- 払い終えたら外わくに `pointer-events: none` を付け、閉じ終わるまでに押されたぶんはうしろの画面へ通す。途中で戻したとき（払いが足りない／閉じられない）は元に戻す。
- 新しく「左端から払って戻る」画面を作るときも、`OverlayScreen` の中で `useEdgeSwipeBack` を使えば自動でそろう。自前の暗がりを置く場合は `data-ft-scrim` を付けること。

### 閉じるときの待ち時間（2.10.3〜2.10.4）

- 2.10.3〜2.10.4 の経緯：退場の動き（`anim-sheet-out` / `anim-fade-out`）を `GLOBAL_CSS` で**空**にしたのに、`useClosing` は180〜240ms待ってから閉じていた。待つあいだ画面が止まって見え、「キャンセル」「✕」「戻る」「暗がりを押す」だけがもたついていた。そこで 2.10.4 から**その場で閉じる**形にし、待ち時間の引数を無くした。
- **この「空のまま」は 2.17.3 で終わり。** 退場の動きを入れたので、待ち時間も入れ直してある（下の「2.17.3〜」）。**動きと待ち時間は、いつも同じ長さにすること。** 片方だけ入れると、2.10.3 の「止まって見える」が戻る。
- 「戻る」の「ひと呼吸」（60ms）は `TapButton` の役目で、こちらはそのまま。

**全画面の退場（2.17.0〜）**　⚠️ 「戻る」「キャンセル」で画面がパッと消えていたのを直した

- `useClosing(onClose, ms)`：**`ms` を渡したときだけ**、`closing` を true にして退場のクラスへ切り替え、`ms` 待ってから `onClose` を呼ぶ。`ms` を渡さないときは、待たずにその場で閉じる（`closing` はいつも false。いまは `useEdgeSwipeBack` に渡す `closeInstant` だけがこの形）。
- 全画面（`DayScreen` / `PlanDashboard` / `FolderDetail` / `SettingsScreen` / `TagManageScreen` / `BackupScreen` / `HelpScreen` / `RecordForm` / `StepForm`）は `useClosing(…, FT_EXIT_RIGHT_MS または FT_EXIT_BOTTOM_MS)` を使い、`closing` を `OverlayScreen` に渡す。**渡し忘れると、また黙ってパッと消える**（2.16.x までの `RecordForm` / `StepForm` はこれだった）。
- **左端から払って戻る（`useEdgeSwipeBack`）には、`ms` なしの別の `useClosing`（`closeInstant`）を渡す。** 指について動く分がすでに退場の動き。同じ `close` を渡すと、退場のクラスが指の動きの上から `transform` を奪い、画面がいったん中央へ跳ね戻って見える。
- **保存して閉じる（`RecordForm`）**：保存はすぐ済ませ、閉じる部分だけを動きのあとへ回す（`saveRecord` の `opts.animateClose`）。保存そのものを動きの後ろへ回さない（動いている間にアプリが背面へ回ると保存が落ちる）。
- 動きを止めているとき（`.ft-still` / 視差効果を減らす）は `motionIsOff()` で待たずに閉じる。
- 退場は `forwards`（外れるまで動き終わりの位置を保つ）。登場の `forwards` 禁止とは別（閉じかけの短い間だけ使う箱のため）。
- 確かめ方：各全画面で「戻る」「✕」「キャンセル」「保存」を押し、右／下へ滑って消えること。左端から払って戻るとき、中央へ跳ね戻らないこと。画面の動きをオフにすると、待たずにすぐ閉じること。

**紙・小窓の退場（2.17.3〜）**　⚠️ 「✕」「保存」で紙がパッと消えていたのを直した

- 紙（シート）は下へ滑って消える（`FT_EXIT_SHEET_MS` 300ms）、その場で薄く出た小窓はその場で薄れて消える（`FT_EXIT_FADE_MS` 200ms）。`GLOBAL_CSS` の `--ft-enter-sheet` / `--ft-enter-fade` と**必ず同じ値**にすること。
- 書き方は、全画面と同じ thunk 方式にそろえた。**`useClosing(onCancel)` の形に戻さないこと**（`onCancel` だけが動き、「保存」「決定」が動かない）。

```js
const [closing, requestClose] = useClosing((fn) => fn(), FT_EXIT_SHEET_MS);
const close   = () => requestClose(onCancel);          // ✕ ・キャンセル・背景
const confirm = () => requestClose(() => onConfirm()); // 保存・決定・選ぶ
```

- **閉じる道は、ぜんぶ `requestClose` に通すこと。** ✕・キャンセル・暗がりだけ動かすと、「保存」を押したときだけパッと消えて見える。一覧から選んで閉じるもの（`TypePickSheet` の `onPick`、`MoveItemSheet` の移し先、`WheelSheet` の「決定」「選択解除」）も同じ。
- 直した部品：`SheetDialog`（「計画の設定」など）／`WheelSheet`（＝`MonthJumpSheet` / `TodayPeriodSheet` / `DrumSelect`）／`DateInput`／`MultiDateSheet`／`TagPickDialog`／`TypePickSheet`／`MoveItemSheet`／`FolderSetupSheet`／`CropSheet`／`ConfirmDialog`／`NameDialog`／`DraftCard`／お祝いの画面。
- `CropSheet` は、切り抜きが終わってから動かす（作っているあいだは開いたまま）。`onConfirm` が promise を返すものは、終わるまで `closing` が戻らない（`useClosing` の作り）。
- 確かめ方：紙を開いて「✕」「キャンセル」「背景」「保存」「決定」を順に押し、**どれでも同じように下へ滑る**こと。まん中・上に出る小窓は、下へ滑らずその場で薄れること。キーボードを出したまま「保存」を押して、紙が一瞬落ちてから滑りはじめないこと。画面の動きをオフにすると、待たずにすぐ閉じること。

### 紙・小窓を開くと、いちばん下の一覧が透ける（2.11.11〜）　⚠️ うしろの留め方を overflow:hidden だけに戻さないこと

- **起きていたこと**：記録の入力画面（`RecordForm`）から「タグを選択」（`TagPickDialog`）を開くと、紙のまわりやキーボードのきわに、入力画面ではなく**そのさらに下の一覧（Today など）のカード**が透けて見えた。
- **原因（見立て）**：背景色ではない。入力画面・右から出る画面の中身は、すべて `bg-white` / `bg-app` で不透明（2.11.11 で全画面ぶん確かめた）。
  - うしろの留め方（`useLockBackground`）が `body { overflow: hidden }` だけだった。**iPhone はこれでもページを送る。** 紙の中の入力欄に触れてキーボードが出ると、ページ（＝一覧）が送られ、`fixed` の層（入力画面・紙）ごとずれて、あいた所に一覧がのぞいた。
  - しかも留める仕組みを通っていたのは `OverlayScreen` / `SheetDialog` / `ConfirmDialog` / `NameDialog` / `DraftCard` / `PhotoViewer` だけで、`TagPickDialog` などほかの紙は何も留めていなかった。
  - Chromium（Mac・Android）では起きない。**確かめは iPhone 実機（ホーム画面から開いたもの）で行うこと。**
- **いまの作り**
  - `useLockBackground` は、開いた瞬間のページ位置を覚えて **`body` を `position: fixed; top: -位置` で留め**、最後の1枚が閉じたら元の位置へ `scrollTo` で戻す。`useLayoutEffect` で行う（`useEffect` だと一覧が一瞬いちばん上へ跳ぶ）。
  - **重なって出るものは、すべてこれを通す。** hook を直接呼べない場所（`open && (...)` の中など）では、外わく（`ft-sheet-wrap`）の最初の子に `BackgroundLock` を置く。2.11.11 で足した場所：`WheelSheet` / `DateInput` の紙 / `MultiDateSheet` / `TagPickDialog` / `TypePickSheet` / `MoveItemSheet` / `CropSheet` / 計画のお祝い / `FolderSetupSheet` / `SideMenu`。
  - 念のため `[data-ft-overlay]` に `box-shadow: 0 0 0 100vmax #FFFFFF`（GLOBAL_CSS）。外わくの**外がわだけ**を白で埋める。内がわは変えないので、左端から払って戻るときにうしろが見えるのはこれまでどおり。`html` の地も `#FFFFFF` にした（app.css）。
- **やってはいけないこと**
  - ❌ **`useLockBackground` を `overflow: hidden` だけに戻さない。**
  - ❌ **新しい紙・小窓・メニューを、うしろを留めずに出さない。** `ft-sheet-wrap` を書いたら `BackgroundLock` か `useLockBackground` があるか `grep` で確かめる。
  - ❌ **紙の中身の背景（`bg-white`）や、全画面の中身の背景（`bg-white` / `bg-app`）を外さない。** 暗がり（`bg-black/25` など）は半透明が正しい。不透明にするのは「中身の箱」のほう。
  - ❌ **`[data-ft-overlay]` の影を、内がわ（`inset`）や外わくの背景色に変えない。** 払って戻るときに、うしろの画面が見えなくなる。
- **確かめ方（iPhone 実機）**：Today を下のほうまで送る → ＋ → メモ → 「タグを追加」→ 「さがす」に触れてキーボードを出す → 紙のまわり・キーボードのきわに一覧が見えないこと。閉じたら Today が**送っていた位置のまま**であること。日付・時刻のドラム、計画の選択、フォルダの「記録を入れる」、名前の入力でも同じように確かめる。

---

### ヘッダーの写真は「帯と同じ形」で切り抜く（2.11.21〜）　⚠️ `aspect` を決め打ちしないこと

- 症状：切り抜きで決めた範囲と、実際に出る帯の範囲が違う。帯は `background-size: cover` ＋ `center` なので、16/9 で切り抜くと上下が切られ、まん中の帯しか出ない。
- 直し方：`useHeaderAspect()`（`headerBandAspect()`）で **いまの帯の形（画面の幅 ÷ `--ft-head-h`）** を測り、`CropSheet` の `aspect` と表示設定のお手本の両方に同じ値を渡す。形がそろっていれば、決めた範囲がそのまま出る。
- **帯の高さを決め打ちしないこと。** 文字の大きさ・端末の上の余白で変わる。`--ft-head-h` は `ScreenHeader` が書いている。画面を回したときは測り直す。
- 測れないとき（変数が無いなど）は 16/5 でおよその形にする。2〜8 の外には出さない。
- すでに保存してある写真（前の版の 16/9）はそのまま使える。まん中に寄せて出る。

### 切り抜きの窓は、外へ出たら戻ってくる（2.11.21〜）

- 動かすぶん（`rubber`）に加えて、**つまんで大きさを変えるぶんにも同じ手ざわりを入れた**（`rubberScale`）。1倍より小さく・4倍より大きくしても、少しだけ外へ出て、指を離すと戻る。
- **指で引いているあいだは動きを付けないこと。** 絵が指から遅れてついてきて、酔ったような感じになる。`start.current` があるあいだは `transition: none`。
- 戻る動き（位置と大きさ）は `.22s cubic-bezier(.22,1,.36,1)`。

### ボタンの :hover / :focus の残り、写真ビューアのボタン（2.11.23〜）　⚠️ hover: は必ず @media の中に書くこと

- 症状：タップしたあとも、ボタンの色が変わったまま残る。ボタンが1回で効かないことがある。
- **:hover**（app.css・GLOBAL_CSS）
  - iPhone / iPad は、指で触れると `:hover` を付け、ほかを触れるまで外さない。以前は `hover:` の道具（`hover:bg-neutral-100` 30か所ほか）と `.bg-ink:hover` が、マウスの無い端末にも付いていた（修正前は最上位の `:hover` が16個）。
  - いまは16個すべてを `@media (hover:hover) and (pointer:fine)` の中に入れた（元の場所のまま包んだので、読み込み順は変わらない）。`.ft-tap-card:hover::after` はもとから同じ囲いの中。
  - ❌ **新しい `hover:` の道具を、囲いの外に書かない。** 実アプリで、最上位に `:hover` が残っていないかは次で見る：`[...document.styleSheets].flatMap(s=>[...s.cssRules]).filter(r=>r.selectorText?.includes(':hover'))` が空であること。
- **:focus**（app.css）
  - 以前は `button:focus{outline:none}` で、キーボード（Tab）で来てもどこにいるか見えなかった。いまは `button:focus:not(:focus-visible){outline:none}` ＋ `:focus-visible` で、**キーボードのときだけ** 2px の枠を出す（指・マウスで当たったフォーカスは枠を出さない）。
  - ❌ `button:focus{outline:none}` に戻さない。かといって `button:focus` で枠や色を付けない（タップのあと残る）。
  - ❌ **タップのたびに `blur()` を呼ばない。** キーボードの上の帯（＋ ^ v ✓）の操作で、入力欄のフォーカスを奪ってキーボードが閉じる。
  - 入力欄（input / textarea / select）は変えていない。`focus:ring-…`（`inputCls`）で見せている。
- **写真ビューアの ✕ と「もとの大きさ」が動かなかった**（`PhotoViewer`、マウス・トラックパッド）
  - 原因：この2つのボタンは、指を受ける箱（`wrapRef`）の**中**にある。`onDown` が `wrapRef.setPointerCapture` を呼ぶと、離したときの `click` はボタンではなく箱へ届く。ドラム（`WheelSheet`）で「行の onClick に頼らない」としているのと同じ仕組み。修正前は、✕は「一度たたき」の道（280ms 遅れ）で閉じていただけで、「もとの大きさ」は完全に無反応だった。
  - 直し方：`onDown` で `e.target.closest("button")` なら受けずに返す。`onUp` は `s.pts` に無い指（ボタンから始まったもの）を無視する（無いと前の `s.moved` で「一度たたき」になり、ビューアごと閉じる）。
  - ❌ **指を受ける箱（`setPointerCapture` を呼ぶ箱）の中に、ボタンを置くなら、その箱の `pointerdown` でボタンを見分けて返すこと。**
  - 指（タッチ）では Chromium の再現では届いていた。iPhone の Safari では未確認。
- **あえて変えていないこと**
  - ❌ **`TapOnceButton` / `useTapOnce` を、ふつうの `onClick` へ統一しない。** 6-③のとおり、iOS は素早く続けて押すと `click` を配らないので、`pointerup` で受けている。`touchstart` と `click` を両方受けている所は無く、アプリ側の `touchstart` / `touchmove` はすべて passive で `preventDefault` も呼んでいない。
  - `touch-action: manipulation` は、`button, [role="button"], label, a` にすでに付いている。
- **確かめ方（iPhone 実機）**：一覧の行・下タブ・ヘッダーのボタンを1回ずつ押し、押したあとに色が残らないこと。写真つきの記録で、写真を開いて ✕ と「もとの大きさ」を押すこと。

### 起動画面のアイコンが下へずれる（2.11.24〜）　⚠️ 中身を画面の高さで中央寄せに戻さないこと

- 症状：ホーム画面から開くと、起動画面のしるし（アイコン）が、出たあと下へずれる。**2.11.22 の手当て（紙を送れなくする）では直らなかった。**
- 実機の写し2枚を測って分かったこと（iPhone 16 Pro、402×874 CSSpx）
  - しるしの中心が、画面高さの 42.6% → 45.5% へ動く（**約25.5px 下へ**）。
  - しるしの上端〜版数の下端の高さは 0.1790 → 0.1795 で**変わらない**。中身は組み直されておらず、かたまりのまま平行移動している。
  - ホームインジケータの位置（0.9883 / 0.9882）で2枚を突き合わせてあるので、測り方は信頼できる。
- 原因：中身を `justify-content: center` で「**本体の高さ**のまん中」に置いていた。iPhone はホーム画面から開いた直後、本体（`window.innerHeight`）の高さがいちど変わる。まん中に置いたものは、高さが Δ 変われば Δ/2 動くので、約51px の変化が約25px のずれになる。高さ 823→874 を作った試しで +25.5px となり、実機の実測と一致した。
- 直し方（index.html）：中身を `.box` にまとめ、ホーム画面から開いたとき（`navigator.standalone` / `display-mode: standalone`）は `screen.height / 2` の位置に `position: absolute` で留める（`transform: translateY(-50%)`）。`screen.height` は端末の画面そのものの高さで、起動のあいだ変わらない。**描かれる前に、その場で**留めること。
- **やってはいけないこと**
  - ❌ `#app-splash .box` を flex の中央寄せへ戻さない（同じずれが戻る）。
  - ❌ `window.innerHeight` / `100vh` / `100lvh` / `100dvh` を、中身の位置の基準にしない。高さが変われば中身も動く。**入れ物（`#app-splash`）の高さをこれらで決めるのは、これまでどおりでよい**（白い地を画面いっぱいに広げるためで、中身の位置には関わらなくなった）。
  - ❌ 向きの固定（manifest の `orientation: portrait`）を外すなら、ここも測り直すこと。`screen.height` は向きで入れ替わらないことがある。
- ブラウザのタブで開いたときは留めない（上下に帯がある分、画面のまん中と本体のまん中が食い違うため）。タブでは中央寄せのまま。
- 確かめたこと：高さを 823→874 に変えても、しるしは 0.0px しか動かない。落ち着いたあとの位置は修正前と同じ（397.4px）で、見た目は変えていない。
- **確かめていないこと**：iPhone 実機。高さが変わること自体は実機の写しから逆算した値で、iOS がなぜ約51px 変えるのかは分かっていない。ただし直し方は「高さが変わっても動かない」形なので、変化の理由によらず効く。

### 起動画面のアイコンが、上から下へずれて見える（2.11.22〜）　⚠️ 起動中に html / body を送れる状態にしないこと

- 症状：起動画面のしるし（アイコン）が、出た直後に上から下へ動いて見える。
- 見立て：起動画面（`position: fixed`）のうしろで、本体（`#root`）は `visibility: hidden` のまま高さを測れる状態で置いてある。中の一覧が長いと、起動中にページ自体が送れる状態になる（修正前は `scrollHeight` 4914 / `scrollY` 300 になることを確かめた）。`viewport-fit=cover` のまま送れる状態だと、iPhone が画面の高さを測り直し、fixed のものがずれて見える。3章「下タブが画面ごとに上下する不具合」（2.11.16〜）と同じ仕組み。
- **確かめられていないこと**：ずれそのものは iPhone の Safari 固有で、Chromium では再現しない（アイコンの座標は修正前も変わらない）。**iPhone 実機（ホーム画面から開いたもの）で、更新後に確かめること。**
- 直し方（index.html）
  - `body.mt-booting { overflow: hidden }`、および起動直後に `document.documentElement.style.overflow = "hidden"`。`mtShowApp()` で両方いっしょに外す。
  - `#app-splash *` に `transition: none !important; animation: none !important;`。動くのは `#app-splash` 自身の薄まり（消えるとき）だけ。
- **やってはいけないこと**
  - ❌ `mtShowApp()` 以外の場所で `overflow` を外さない（12秒の歯止めも `mtShowApp` を通るので、戻し忘れは起きない）。
  - ❌ 直らなかったからといって `#root` を `display: none` にしない（ヘッダーの高さが 0 で測られる）。

### 起動画面（アイコン・版数）のときに、下の帯が一瞬見える（2.11.20〜）　⚠️ `body.mt-booting` を外さないこと

- 症状：起動画面が出ているあいだ、画面のいちばん下（ホームバーのあたり）に下の帯（フッター）が一瞬のぞく。
- 原因：アプリ本体は読み込みの途中でも先に描かれ、下の帯も出る。起動画面はその上に重なっているはずだが、iPhone では画面の下の端や、起動画面が出る前後の一瞬に、帯だけが透けて見える。
- 直し方（index.html）
  - **起動画面が消えはじめるまで、本体（`#root`）を見えなくする。** `body` に `mt-booting` を付けておき、`__hibiHideSplash` が薄くしはじめる瞬間に外す。見えなければ透けようがない。
  - **`display: none` にしないこと。** ヘッダーの高さを測る部品が 0 のまま測ってしまう。`visibility: hidden`（＋`pointer-events: none`）で隠す。
  - **起動画面の四辺を明示し、高さは大きいほうの画面（`100lvh`）まで取ること。** `inset: 0` だけだと端末によって下の端が数十px 空き、そこから帯がのぞく。
  - 保険として、12秒たてば本体を必ず出す（読み込み終わりの知らせが来なくても真っ白なままにしない）。
- **`__hibiHideSplash` の二度目の呼び出しで、その場で本体を出さないこと。** 起動直後に一度、記録を読み終えたあとにもう一度呼ばれる。二度目で出すと、まだ起動画面が出ているうちに本体が見えてしまう。一度目が用意した時計（`MT_SPLASH_MS` = 1400ms）に任せる。

### キーボードを出したまま、入力画面の下まで送れない（2.11.17〜）　⚠️ 送り場の下の「キーボードぶんの余白」を外さないこと

- **起きていたこと**：記録の入力画面（`RecordForm`）で本文を書いているとき、キーボードの下に隠れた部分（本文の枠の下など）へ送れなかった。送っても指を離すと戻ってきた。
- **原因**：iPhone はキーボードを出しても `fixed` の画面の高さを変えない。送り場（`flex-1 overflow-y-auto`）の下の `pb-28` だけではキーボード（＋ ^ v ✓ の帯）の高さに足りず、いちばん下まで送っても最後の部分がキーボードの裏に残った。それ以上送ろうとすると指がページまで届き、`useLockBackground` が引き戻すので「戻ってくる」ように見えた。
- **いまの作り**
  - `installKeyboardInset`（`useLockBackground` のすぐ下、読み込み時に1回だけ動く）が `visualViewport` から「レイアウトの高さ − 見えている高さ − 見えている上端」を測り、`--ft-kb` に入れる。60px 未満は 0。
  - GLOBAL_CSS の `[data-ft-overlay] .flex-1.overflow-y-auto::after` が、その高さぶんの余白を送り場の最後に足す。`::after` にしているのは、画面ごとに違う `pb-28` / `py-5` / `pad-fab` を上書きしないため。
  - **余白はキーボードが出る「前」に足す（2.11.18〜）。** 入力欄に指が触れた時点（`touchstart` / `pointerdown`、capture）と `focusin` で、前回のキーボードの高さ（はじめは画面の高さの45%）を**その場で**足す。余白は送り場の最後に足すだけなので、足した瞬間に見た目は動かない。
  - キーボードが出たあとは実際に測った高さに合わせる。キーボードが出てくる途中（まだ測れない）あいだ、入力欄にいるなら余白を消さない。閉じたら 0 に戻す。
  - **なぜ前もって足すのか**：2.11.17 では、キーボードが出たあとで余白を足し、そのあと送り場をこちらで送っていた。iPhone が入力欄を見せようとした瞬間に送り場に余地がなかったので、iPhone はページごと送り、それを `useLockBackground` が引き戻して「画面が下がって、また戻る」動きが出ていた。先に余白があれば、iPhone は送り場の中だけを送るので、動きは一度で済む。
- **やってはいけないこと**
  - ❌ **`useLockBackground` の引き戻しをやめて直さない。** 一覧が透ける件（2.11.11）が戻る。
  - ❌ **キーボード対策で `window.scrollTo` / ページを送らない。** 引き戻しとぶつかって画面が震える。送るのは送り場（`.overflow-y-auto`）のほう。
  - ❌ **余白を `requestAnimationFrame` や `setTimeout` のあとで足さない。** iPhone の送りに間に合わず、「下がって戻る」動きが戻る。
  - ❌ **キーボードが出たあとで、こちらから送り場を送る処理を足さない。** iPhone の送りと二重になる。
  - ❌ 新しい全画面の送り場を作るときは `flex-1 overflow-y-auto` の形にそろえる（ほかの形だと余白が足されない）。
- **確かめ方（iPhone 実機）**：＋ → スケジュール → 本文に何行か書いてキーボードを出したまま、下へ送る → 本文の枠の下（そのあとの項目）まで見えて、指を離しても戻らないこと。キーボードを閉じたら余分な余白が消えていること。本文の下のほうの行に触れてキーボードを出したとき、画面が一度下がってから戻るような動きがないこと（動くのは入力欄をキーボードの上へ寄せる一度だけ）。

### 下から出る紙の入力欄が、キーボードの裏に隠れる（2.11.26〜）　⚠️ 紙はキーボードの上へ持ち上げること

- **起きていたこと**：フォルダ一覧で長押し →「フォルダの設定」で名前に触れると、紙ごとキーボードの裏に入り、入力欄が見えなかった。
- **原因**：上の件（2.11.17）の余白は、全画面（`[data-ft-overlay]`）の送り場にしか効いていなかった。下から出る紙（`.ft-sheet-wrap.items-end`）は上下 0 で画面に留めてあり、iPhone はキーボードを出しても `fixed` の高さを変えないので、紙の足もと＝入力欄のある場所がまるごとキーボードの裏に残った。`useLockBackground` がページの送りを引き戻すので、iPhone が自分で寄せることもできない。
- **同じ形で隠れていた場所**（どれも `.ft-sheet-wrap` の下寄せの紙に入力欄がある）：
  - `SheetDialog` を使う紙：フォルダの追加・設定、計画の名前とアイコン（`NameIconSheet`）、計画の設定（`PlanSettingsSheet`）、バックアップの合言葉・文字から読み込む（`BackupScreen`）
  - タグを選ぶ（`TagPickDialog`）の「さがす／新しいタグ」
  - フォルダの条件と記録の出し入れ（`FolderSetupSheet`）
  - 記録の入力画面で計画を選ぶ紙（`PlanSelect` の「計画をさがす」）
- **直し方**（まとめて `.ft-sheet-wrap` で直してある。紙ごとの手当てはしていない）：
  - `installKeyboardInset` が、紙の中の入力欄も数えるようにした（`isTyping` の対象に `.ft-sheet-wrap` を足した）。~~触れた時点で前回のキーボードの高さを先に足すのも、全画面と同じ。~~ → **2.13.1 で改めた。紙は `focusin` から持ち上げる（下の「紙の入力欄に触れると、紙が跳んで閉じる」）。**
  - 見える窓の上端を `--ft-vv-top` に、キーボードが出ている印を `html[data-ft-kb]` に入れる。
  - GLOBAL_CSS で、キーボードが出ているあいだだけ、下寄せの紙の外わくに `padding-bottom: var(--ft-kb)`（上は `--ft-vv-top`）を足して持ち上げる。まん中の小窓は下だけ足す。
  - 紙の高さの上限（82% / 92%）を、キーボードのあいだだけ「見える高さいっぱい」にゆるめる。`SheetDialog` の中身は `overflow-y-auto` で送れるようにした。
  - 紙の足もとの安全域（`SAFE_BOTTOM`）は、キーボードのあいだだけ 0（`--ft-sab`）。ホームバーはキーボードの裏なので。
  - 指で触れない端末（マウス・外付けキーボード）では、紙の中で先に余白を足さない。念のため、足してから 1.2 秒たってもキーボードが出ていなければ 0 に戻す。
- **やってはいけないこと**
  - ❌ **外わく（`.ft-sheet-wrap`）の top / bottom を縮めて持ち上げない。** 暗がりまで縮み、キーボードの角のすき間から暗くなっていない一覧がのぞく。内側の余白で持ち上げる。
  - ❌ **持ち上げに transition を付けない。** 遅れると iPhone がページを送り、引き戻しと二重に動く（2.11.18 と同じ）。
  - ❌ 新しい紙を `.ft-sheet-wrap` 以外の形で作らない。作ると、この持ち上げが効かない。
- **確かめ方（iPhone 実機）**：フォルダを長押し →「フォルダの設定」→ 名前に触れる → 紙がキーボードの上に出て、打った文字と「保存」が見えること。フォルダの追加（右下の＋）、計画の名前、タグをさがす、計画をさがす、バックアップの合言葉でも同じように確かめる。キーボードを閉じたら、紙が元の位置（画面の下）に戻ること。

---

### 紙の入力欄に触れると、紙が上へ跳んで、キーボードが出ないまま閉じる（2.13.1〜）　⚠️ 紙を「触れた時点」で持ち上げないこと

- **起きていたこと**：フォルダを長押し →「フォルダの設定」で名前の欄に触れると、紙だけが上へ跳ね上がり、キーボードは出ず、そのまま紙が閉じた（名前を変えられない）。
- **原因**：2.11.26 で、紙の中の入力欄でも「指が触れた時点（`touchstart` / `pointerdown`）」にキーボードぶんを先に足すようにした。全画面の送り場は**いちばん下に余白を足すだけ**なので見た目は動かないが、紙は外わくの `padding-bottom` で**紙ごと上へ動く**。
  - 指が触れたまま紙が約45%ぶん持ち上がる → 指を離した場所には入力欄がない。
  - iPhone はそのタップを、その時点で指の下にあるもの（暗がり）へ配る。入力欄に入れないのでキーボードは出ない。
  - 暗がりの `click` は外わくの `onClick: close` へ届き、紙が閉じる。
- **いまの作り**
  - `installKeyboardInset` の `reserve` は、紙（`.ft-sheet-wrap`）の中の入力欄では `touchstart` / `pointerdown` を無視し、**`focusin` で持ち上げる**（その場で。rAF・setTimeout は使わない）。全画面の送り場はこれまでどおり触れた時点で足す。
  - 念のための歯止め：`focusin` は iPhone の mousedown の中で起きるので、その直後の mouseup / click は持ち上がった後の位置で当たり直す。**「紙の入力欄で押し始めたのに、入力欄の外で click になった」ものだけ**を、document の capture で止める（`noteDown` / `guardClick`）。暗がりで押し始めたタップは止めないので、暗がりを押して閉じる操作はそのまま。
- **やってはいけないこと**
  - ❌ **紙の中の入力欄で、`touchstart` / `pointerdown` の時点で `data-ft-kb` を立てない（紙を持ち上げない）。** 指の下から入力欄が逃げる。
  - ❌ **`guardClick` を外さない。** `focusin` での持ち上げでも、同じタップの click が暗がりに当たって閉じることがある。
  - ❌ 持ち上げを `setTimeout` などで遅らせて直さない（2.11.18・2.11.26 の「ページが送られて引き戻される」動きが戻る）。
- **確かめ方（iPhone 実機、ホーム画面から開いたもの）**：フォルダを長押し →「フォルダの設定」→ 名前に触れる → キーボードが出て、紙がキーボードの上に出ること。紙が閉じないこと。続けて文字を打てて「保存」できること。暗がりを押せば紙が閉じること。フォルダの追加（右下の＋）、計画の名前、タグをさがす、計画をさがす、バックアップの合言葉でも同じように確かめる。

### 紙の入力欄とキーボード：滑らかに持ち上げ、入力欄をキーボードの上に出す（2.15.0〜）

> 2.14.0 で、打つ欄のある紙を「上寄せの小窓」に変えたが、**下から出る紙のほうが使いやすいので 2.15.0 で紙に戻した。**
> 2.14.0 の `DialogFrame` / `FormDialog` / `.ft-dialog-*` は入っていない。紙の持ち上げは 2.11.26・2.13.1 のしくみの上に、下の改善を足した形。

- **決まり**：紙の入力欄に入ると、紙がキーボードの上まで持ち上がり、**入力欄の下の端がキーボードより上に来る**こと。
- **いまの作り**（すべて `installKeyboardInset` の中）
  - **滑らかな持ち上げ（FLIP）**：`setKb` が余白（`--ft-kb` / `data-ft-kb`）を変える前後で、下寄せ・まん中の紙の箱の位置を測り、差のぶんだけ `transform` を当てて 250ms で 0 へ戻す（Web Animations の `animate`、fill なし）。**位置そのものはその場で変わる**ので、iPhone が入力欄の位置を読むときには、もう持ち上がっている。
    - `focusin` からのときは、動きを `requestAnimationFrame` で始める（iPhone は focus の中で入力欄の位置を読むので、そこへ transform を混ぜない。rAF は描く前に走るのでちらつかない）。
    - 動いている最中にもう一度変わったら、途中の見えている位置から続ける。
    - 中にさらに紙を重ねている箱（`FolderSetupSheet` の上の `TagPickDialog` など）は動かさない（transform の箱の中では fixed がずれる）。
    - 「画面の動き」オフ（`.ft-still`）／視差効果を減らす、では動かさない。
  - **キーボードの高さを覚える**：~~測れた高さを `localStorage` の `mt-kb-last` に入れる~~ → **2.16.0 で改めた**（下の節）。覚える値が小さすぎて、揺れがくり返していた。
  - **入力欄を見えるところまで送る**（`reveal`）：キーボードの高さが測れてから 290ms 後（紙が上がりきったあと）、入力欄が紙の中の送り場（`overflow-y: auto`）の見えている範囲に入っていなければ、**送り場だけ**を送る。入っていれば何もしない（iPhone が先に送っていても二重にならない）。キーボードを出したまま別の入力欄へ移ったときも同じ。
  - **打っている最中に暗がりを押したら、まずキーボードだけを閉じる**（`scrimTap`）。紙まで閉じると打ちかけの文字が消えるので。もう一度押せば紙が閉じる。✕・キャンセル・保存はそのまま効く。押し始めた時点（touchstart / pointerdown）で打っていたかで決める（端末によっては、暗がりに触れた時点で入力欄から外れる）。
  - **`WheelSheet` を見える高さに収める**：`flex-col` と `ft-sheet-box` を付けた。以前は「計画を選択」（`PlanSelect`）の紙がキーボードで持ち上がると、見出しと「計画をさがす」が画面の上へはみ出していた。`PlanPickList` は `fill` で、一覧の高さ（42vh）がキーボードのあいだだけ縮む（最小 96px）。
- **やってはいけないこと**
  - ❌ **余白（padding）に transition を付けて滑らかにしない。** 動かすのは FLIP の transform だけ（2.11.18・2.11.26 の理由）。
  - ❌ **FLIP の transform に fill（forwards / both）を付けない。** 終わったあとも箱に transform が残り、中の fixed がずれる。
  - ❌ **`reveal` で window / ページを送らない。** 送るのは紙の中の送り場だけ。全画面の入力欄にも使わない。
  - ❌ **紙を、入力欄に指が触れた時点で持ち上げない**（2.13.1）。
  - ❌ `WheelSheet` の箱から `flex-col` / `ft-sheet-box` を外さない。
- **確かめ方（iPhone 実機、ホーム画面から開いたもの）**：フォルダの設定・フォルダの追加・計画の追加・計画の「名前・色・絵」・記録の「タグを追加」・「計画を選択」・フォルダの「記録を入れる」→「手動で入れる」のキーワード・バックアップの合言葉で、入力欄に触れる。
  - 紙がキーボードといっしょに滑らかに上がり、入力欄の下の端がキーボードより上にあること。見出し（題と ✕）も画面の中に見えていること。
  - 文字を打ってから暗がりを押す → キーボードだけが閉じ、紙と文字は残ること。もう一度押すと紙が閉じること。
  - キーボードを閉じると、紙が滑らかに下へ戻ること。
  - 2回め以降に開いたとき、紙が一度で止まること（途中でもう一度ずれない）。

### キーボードが出るとき画面ぜんたいが揺れる・紙の下から一覧がのぞく・キーボードの閉じ方（2.16.0〜）

- **起きていたこと（2.15.0 の実機）**
  1. 入力欄に触れてキーボードが出るとき、画面ぜんたい（ヘッダーごと）が一度下がってから上がる。
  2. 下から出る紙がキーボードで持ち上がったとき、紙の下にすき間ができ、うしろの画面が見える。
- **原因**
  1. iPhone は、focus の時点で入力欄がキーボードの縁より下にあると、画面ぜんたいをずらして見せる。こちらは測り直して紙を動かし、iPhone はまたずらし戻す ── これが揺れ。focus の時点で紙を「覚えておいたキーボードの高さ」まで先に上げているが、**覚える値がまちがっていた。** 2.15.0 は「レイアウトの高さ − 見えている高さ − 見えている上端（offsetTop）」を覚えていたので、iPhone が一度ずらすと、本当のキーボードより小さい値が残る。次に開いたときも持ち上げが足りず、揺れがくり返した。
  2. 紙はキーボードより少し早く上がりきり、キーボードより遅れて下りる。見込みの高さが本当のキーボードより大きいときも、紙とキーボードのあいだがあく。そのすき間から、暗がりとうしろの一覧が見えていた。
- **いまの作り**（`installKeyboardInset` と GLOBAL_CSS）
  - **覚えるのは「キーボードそのものの高さ」**（レイアウトの高さ − 見えている高さ）。`mt-kb-last` に `{"p": 縦の高さ, "l": 横の高さ}` の形で、向きごとに入れる。2.15.0 の値（数字ひとつ）は読み捨てる。まだ覚えていないときの見込みは画面の高さの 50%（**少なめに見込まない**。多めなら紙が少し下がって落ち着くだけ）。
  - 紙の下の余白（`--ft-kb`）は、これまでどおり offsetTop を引いた値（レイアウトの下から隠れている高さ）。覚える値と使う値を分けたこと。
  - **紙の下に白を伸ばす**：`.ft-sheet-wrap.items-end > .relative.rounded-t-2xl::after` が、紙の真下に画面の高さぶんの白を持つ。紙が画面の下についているときは外わくの外なので見えない。FLIP の transform といっしょに動く。
  - **紙を、キーボードといっしょに下ろしはじめる**：紙の入力欄から外れたら（focusout）、次の入力欄へ移ったのでなければ、キーボードが引っ込むのを待たずに `setKb(0)`。測った値がまだ残っていても、紙の入力欄から外れていれば 0 にする。全画面の送り場はこれまでどおり（早く縮めると、送っていた位置がずれる）。
  - 動きの長さ 280ms、曲がり方 `cubic-bezier(.2,.85,.25,1)`（キーボードに近づけた）。
- **キーボードを引っ込める操作（2.16.0〜）**
  - **Enter**：1行の入力欄（input）で Enter を押すと、各画面の Enter の役目（名前の決定・タグを作る・合言葉でひらく・検索する）をすませてから `blur()`。textarea の Enter は改行なので対象外。
    - **日本語入力の「変換を確定する Enter」は無視する。** Safari は確定の Enter を `compositionend` のあとに `keyCode 229`・`isComposing=false` で送ってくる。`isComposing` / `keyCode 229` / `compositionend` から 80ms 以内、のどれかなら確定の Enter とみなし、**各画面の onKeyDown にも届けない**（以前は、変換を確定しただけで名前が決定されることがあった）。
  - **入力欄の外をタップ**：指で押して、ほとんど動かさず（10px 以内）、600ms 以内に離したときだけ `blur()`。送った（スクロールした）ときは閉じない。ボタン・リンク・ほかの入力欄・ラベルなど、押して何かが起きるもの（`KEEP_KB`）は除く（押したものがそのまま効く）。入力欄のまわり 12px は入力欄のうち。紙の暗がりを押したときは、キーボードだけを引っ込め、紙は閉じない（2回めで閉じる。2.15.0 の `scrimTap` のまま）。
- **やってはいけないこと**
  - ❌ **覚えるキーボードの高さから offsetTop を引かない。** 揺れが戻る。
  - ❌ **最初の見込みを少なめにしない**（45% 以下にしない）。
  - ❌ **紙の下の白（::after）を消さない。** 紙の箱に `overflow: hidden` を付けない（白が切れる）。
  - ❌ **`.ft-sheet-wrap` の `overflow: clip`（`@supports` の中）を外さない。** 紙の下の白が外わくの下へはみ出しているので、`hidden` だけだとそこが「送る余地」になる。入力欄に触れた瞬間、ブラウザが入力欄を見せようとして外わくを送り、紙と暗がりがまとめて上へずれたまま戻らなかった（2.16.0 の作業中に見つけた）。`clip` が使えない古い iPhone では、`onAnyScroll` が送られた外わくをすぐ 0 へ戻す（一瞬ちらつくことはある）。
  - ❌ padding に transition を付けて滑らかにしない（2.11.18・2.11.26・2.15.0 のまま）。
  - ❌ Enter で閉じる処理で、`isComposing` だけを見ない（Safari で変換確定のたびに閉じる）。
  - ❌ タップで閉じる処理に `preventDefault` を入れない。`touchstart` / `touchmove` は passive のまま（リンクの件と同じ。画面が送れなくなる）。
- **確かめ方（iPhone 実機、ホーム画面から開いたもの）**
  - いったんアプリを入れ直す。フォルダの設定で名前に触れる → 1回めは紙が一度上がって少し下がることがある。**2回め以降は、紙だけが一度で上がり、ヘッダーやうしろの画面は動かない**こと。
  - 紙が上がる途中・下りる途中・上がりきったあとに、紙の下から暗がりや一覧が見えないこと（白いだけ）。
  - 名前を打って Enter → キーボードが閉じ、紙は開いたまま・文字は残る。紙が滑らかに下りる。
  - かなを打って変換し、Enter で確定 → **キーボードは閉じない**。もう一度 Enter で閉じる。
  - 打っている最中に紙の中の何もないところ（「アイコン」の文字など）を押す → キーボードが閉じる。アイコンを押す → アイコンが選ばれ、キーボードは出たまま。
  - 紙の中や記録の入力画面を指で送る → キーボードは閉じない。
  - 記録の入力画面でタイトルを打って Enter／何もないところを押す → キーボードが閉じる。
  - 横向きでも同じように確かめる（横の高さは別に覚える）。

### キーボードを出したまま紙を払うと、紙が上へ持ち上がる（2.18.3〜）　⚠️ キーボードのあいだの紙の送りは、こちらで行う

- **起きていたこと**：計画の設定で名前を打っている（キーボードが出ている）ときに紙を上へ払うと、紙ごと上へ持ち上がった。
- **原因**：`installKeyboardInset` の指の見張り（`tgMove`）は、紙の中に送れる箱（`.ft-sheet-body` など）があると素通ししていた。キーボードが出ていると、iOS はその箱を送りきった先（や送りはじめ）で**見える窓（`visualViewport`）ごと**送る。これは `window.scrollY` が動かないので、`tgPin` の引き戻しでは戻せなかった。`overscroll-behavior: contain` も効かない。
- **いまの作り**
  - 指が紙に降りた時点（`tgStart`）で、キーボードが出ているか（出かかっている・見えない入力欄で出している途中・紙の入力欄にフォーカスがある、も含む）を `kbUp()` で控える（`tg.kb`）。
  - `tg.kb` のあいだは、紙の中の `touchmove` を**いつも** `preventDefault` し、送れる箱は**こちらで**送る（`kbDrag`）。指が `KB_SLOP`（8px）動いてから縦・横を決め、その向きに送れる箱をひとつ選び、離すまで変えない。`touch-action: none` の部品（ドラム・写真の切り抜きなど）に当たったら何も送らない（部品が自分で扱う）。
  - 離したら指の速さで惰性をつける（`kbFling`）。次に指が降りたら止める。
  - 送ったあとの指の離しで、指の下のボタン（アイコンなど）が押されないよう、350ms だけ click を捨てる（`kbEatClick`）。
  - キーボードが出ていないときは、これまでどおり（送れる箱は iOS にまかせる）。
- **やってはいけないこと**
  - ❌ キーボードのあいだ、紙の中の指の動きを iOS に素通ししない（送れる箱があっても）。
  - ❌ 引き戻し（`window.scrollTo`）で見える窓のずれを戻そうとしない。戻せない。
- **確かめ方（iPhone 実機、ホーム画面から開いたもの）**：計画の一覧で長押し →「計画の設定」→ 名前に触れてキーボードを出す → 紙の見出し・入力欄・アイコンのあたりを上下に払う → 紙が上へ持ち上がらないこと。アイコンの一覧が下にあるときは、払えば中だけが送られ、離すと少し惰性で流れること。払ったあとにアイコンが選ばれないこと。フォルダの設定・タグをさがす・計画を選択（キーボードを出した状態）でも同じ。キーボードを閉じたあとは、これまでどおり送れること。

### キーボードと入力欄 ── いまの作り（2.16.6〜、2.16.7 で紙の固定を足した）　⚠️ キーボードまわりを触るときは、まずここ。下の 2.16.1〜2.16.5 の節より優先

- **いちばん大事な決まり**：iOS は、フォーカスが入った時点の**本物のカーソルの位置**で、画面ぜんたい（ヘッダーごと）を押し上げるかを決める。本物のカーソルを描くのも iOS。だから**どの画面のどの入力欄も、タップで iOS に直接フォーカスさせない。**
- **2.16.5 でずれが戻った理由**：「キーボードに隠れない欄」「押した場所がキーボードより上の textarea」「下タブの画面（探す・計画・フォルダ）の欄」を、iOS のふつうのタップにまかせていた。もうひとつ、保険の「相殺」（`visualViewport.offsetTop` のぶん外の層を下へずらす）が、iOS の報告と実際の動きが食い違う場面で、画面ぜんたいを**下げて**いた可能性が高い。**相殺はやめた。**
- **いまの流れ**（キーボードで打つ欄をタップしたとき。すでに打っている欄のタップ＝カーソル移動だけは iOS にまかせる）
  1. `touchend` で `preventDefault`。
  2. 画面の上のほう（見出しの下あたり）に置いた見えない入力欄（`data-ft-kb-proxy`、`body` 直下・`position: fixed`）にフォーカス → **キーボードだけが先に下から出てくる。** 押した欄にはフォーカス枠だけを出す（カーソルは描かない）。
  3. キーボードが出た知らせ（`visualViewport` の resize）を待つ。ただしタップから `KB_WAIT_MS`（150ms）より前には動かさず、知らせが来なくても `KB_FALLBACK_MS`（420ms）で始める。そこから、押した欄を**実際に測ったキーボードの高さ**の上まで、`KB_UP_MS`（450ms）・`--ease-out` でゆっくり持ち上げる。
     - 紙：紙の箱を CSS の transition（transform）。
     - 全画面：送り場の中身（子）を transform。終わったら `scrollTop` へ置きかえる。見出し・下のボタンの帯は動かさない（ボタンはキーボードの裏）。
     - ページそのもの（下タブの画面など）：同じ曲線でページを送る。ページの最後には送れる余地（`body` の `padding-bottom: var(--ft-kb)`）を先に足してある。
  4. 持ち上がりきったら本物の入力欄へフォーカスを移す（キーボードは出たまま）。打ちかけの文字は本物へ移す。カーソルは文字の最後。
  - 動かさなくてよい欄（もともとキーボードより上）も、同じ流れで本物へ移す（動きは無し）。
- **下ろすとき**：入力欄から外れたら、紙は `KB_DOWN_MS`（360ms）で下りる。全画面・ページは、キーボードが引っ込みきってから元に戻す。
- **紙で打っているあいだは、紙を指で動かさない（2.16.7〜）**
  - 起きていたこと：計画・フォルダの設定でキーボードを出したまま紙の上を払うと、紙ごと画面がずれて崩れ、うしろの一覧まで送られた。
  - 原因1：`useLockBackground` の引き戻し（`overlayPin`）が、2.16.1 から「入力中は引き戻さない」ままだった。2.16.6 からは iOS がキーボードのために画面を送ることはないので、**入力中も引き戻す**ように戻した。
  - 原因2：見える窓が指で動くたび（`visualViewport` の scroll）に、位置を合わせ直していた（`adjustFor`）。いまは**キーボードの高さが変わったとき（と、別の欄へ移ったとき）だけ**合わせ直す（`lastRaw`）。
  - 紙（`ctx === "sheet"`）で打っているあいだだけ、`touchmove` を `passive: false` で受け、**紙の中で実際に送れる箱がその向きに送れるとき以外**は `preventDefault` する。入力欄の上の指の動き（カーソル移動）は止めない。紙の外で始まった指も止めない。リスナーは `setCtx` で付け外しする（常に付けておくと、アプリぜんたいの送りが重くなる）。
  - 全画面（記録の入力）には使わない。欄が伸び、ほかの欄を見ながら書くので、送れるままにしてある。
- **画面のキーボードが出ない端末（外付けキーボードの iPad など、2.16.7〜）**：キーボードの出た知らせを `KB_FALLBACK_MS`＋`KB_FALLBACK2_MS` 待っても来なければ、持ち上げずに本物の入力欄へ移す。
- **速さを変えたいとき**：`KB_WAIT_MS` / `KB_UP_MS` / `KB_DOWN_MS` だけを変える。
- **やってはいけないこと**
  - ❌ どの欄も、タップで iOS にフォーカスさせない（例外を足さない）。
  - ❌ 本物の入力欄に、キーボードの裏になる位置で `focus()` しない。本物に移すのは、持ち上がりきってから。
  - ❌ `offsetTop` のぶん画面を下へずらす「相殺」を戻さない。
  - ❌ 見えない入力欄を `opacity: 0` / `display: none` / `visibility: hidden` にしない。
  - ❌ 押し上げに `window.scrollTo` のくり返しで対抗しない。
  - ❌ `overlayPin`（うしろの引き戻し）に「入力中は止める」を戻さない（2.16.7。紙を払うとうしろが送られる）。
  - ❌ `visualViewport` の scroll のたびに紙や送り場を合わせ直さない（指について動いて崩れる）。
  - ❌ **新しい画面を、決まった形の外で作らない。** 持ち上げられるのは「紙（`.ft-sheet-wrap` の直下の箱）」「全画面の送り場（`flex-1 overflow-y-auto`）」「ページそのもの」だけ。持ち上げられないと、本物の入力欄にキーボードの裏でフォーカスが入り、押し上げが戻る。
- **確かめたこと（Chromium、コマごと）**：フォルダの検索欄・フォルダの追加・スケジュールのタイトル・メモのどれも、見えない入力欄→本物の入力欄の順にフォーカスが移り、ヘッダーは全コマ 0px。紙の入力欄は 638px → 376px を 450ms でゆっくり上がり、メモは中身だけが上がり、下のボタンの帯は動かない。**iPhone 実機では未確認。**
- **確かめ方（iPhone 実機、ホーム画面から開いたもの。入れ直してから）**：探す・計画・フォルダの検索欄、フォルダの設定、計画の名前、タグをさがす、スケジュールのタイトル・場所・メモ（上のほうと下のほう）で、キーボードが先に出て、そのあと欄がゆっくりキーボードの上へ来ること。どの画面でもヘッダーが上にも下にも動かないこと。

### キーボードと入力欄 ── いまの作り（2.16.4〜、2.16.5 でカーソルの絵をやめた）　⚠️ 2.16.1〜2.16.3 の節より優先。キーボードまわりを触るときは、まずここ

- **いちばん大事な決まり**：iOS は、フォーカスが入った時点（とキーボードが出るあいだ）の**本物のカーソルの位置**で、画面ぜんたい（ヘッダーごと）を押し上げるかを決める。本物のカーソルを描くのも iOS なので、「キーボードの裏の位置に本物のカーソルを出す」と必ず押し上げられる。
- **2.16.3 で改めたこと**：紙の複製と逆向きの transform は、実機で重く、カクついた。全画面では下のボタンの帯（キャンセル・削除・保存）まで持ち上がっていた。
- **いまの流れ**（キーボードに隠れる入力欄をタップしたとき。installKeyboardInset の `faEnd`）
  1. `touchend` で `preventDefault`。
  2. **持ち上がった先の位置**に置いた見えない入力欄（`data-ft-kb-proxy`）にフォーカス → キーボードがすぐ出はじめる。
  3. 押した入力欄には、その場でフォーカス枠（`.ft-kb-pending-ring`）だけを出す。**カーソルは描かない（2.16.5〜）。** 本物のカーソルは持ち上がりきってから出る。2.16.4 ではカーソルの絵を描いていたが、仕様が複雑になるのでやめた。❌ 戻さないこと。
  4. 入力エリアを **CSS の transition（transform）だけ**で `KB_ANIM_MS`（280ms）かけて持ち上げる。
     - 紙：紙の箱に `transform`。背の高い紙で上がりきらないぶんは、紙の中の送り場をその場で送る。
     - 全画面：送り場の中身（子）に `transform`。**見出しと下のボタンの帯は動かさない。** 上がりきったら transform を外して同じぶん `scrollTop` を進める（見た目は変わらない）。
  5. 上がりきったら本物の入力欄へフォーカスを移す（キーボードは出たまま）。打ち始めていた文字は本物へ移す。カーソルは文字の最後。持ち上げる量は入力欄の下の端で決める。
- **ふつうのタップのまま（iOS にまかせる）**：キーボードに隠れない入力欄。textarea で、押した場所がキーボードより上のとき（カーソルは押した場所に入る）。
- **キーボードが出きったあと（全画面、`data-ft-kbfix`）**：送り場の箱をキーボードの上端で終わらせる。打っていくうちにカーソルが下へ進んでも、iOS は送り場の中だけを送る。下のボタンの帯は外わくの下（キーボードの裏）に置いたまま。抜けた高さは `--ft-kbfoot` として送り場の最後に残すので、送っている位置はずれない。
- **キーボードの ^ v で移ったとき・高さの見込みが違ったとき**：`adjustFor` が、隠れているぶんだけ紙を上げ下げする（transition）／送り場を送る。全画面の textarea は iOS にまかせる。
- **下ろすとき**：入力欄から外れたら、紙は transition でキーボードといっしょに下りる。全画面はキーボードが引っ込みきってから元に戻す。
- **保険**：それでも押し上げられたら、`visualViewport.offsetTop` のぶんを外の層の `translate` で相殺する（2.16.2 のまま）。
- **やってはいけないこと**
  - ❌ キーボードの裏の位置で、本物の入力欄に `focus()` しない。本物に移すのは、持ち上がりきってから。
  - ❌ 見えない入力欄を `opacity: 0` / `display: none` / `visibility: hidden` にしない（iOS がキーボードを出さない・カーソルを扱わないことがある）。色を透明にして、z-index で奥へ置く。
  - ❌ 下のボタンの帯を、送り場といっしょに縮めて持ち上げない。
  - ❌ padding / 位置に transition を付けない。動かすのは transform だけ。
  - ❌ 押し上げに `window.scrollTo` のくり返しで対抗しない。
- **確かめたこと（Chromium、コマごと）**：紙の入力欄は 638px → 376px をなめらかに上がる。そのあいだキーボードは見えない入力欄で呼ばれている。上がりきった位置で本物にフォーカスが移り、そのまま打てる。全画面は、ヘッダーが全コマ 0px のまま、メモ欄の中身だけが上がり、終わると scrollTop に置きかわる。下のボタンの帯は 801px のまま。キーボードが出きった状態では、送り場がキーボードの上端で終わり、ボタンの帯と中身の位置は変わらない。**iPhone 実機では未確認。**
- **確かめ方（iPhone 実機、ホーム画面から開いたもの。入れ直してから）**
  - フォルダの設定で名前に触れる → 押した欄にすぐ枠が出て、キーボードといっしょに紙がなめらかに上がり、止まったところでカーソルが出る。見出しは画面の外へ出ない。
  - スケジュールで、下のほうのメモに触れる → 見出しは動かず、メモの中身だけがキーボードの上へ上がる。キャンセル・削除・保存はキーボードの裏に残る。キーボードを閉じると元に戻る。
  - 何行も打って、カーソルがキーボードの縁まで来ても、ヘッダーが動かないこと。
  - 上がる途中・入れ替わりの瞬間に、文字や枠がちらつかないこと。

### 紙がキーボードといっしょに滑らかに上がる ──「iOS が読む位置」と「見た目の位置」を分ける（2.16.3〜）

- **起きていたこと（2.16.2）**：押し上げは直ったが、紙の入力欄に触れると、紙がその場で最終位置へ瞬間移動した。
- **決まり**：iOS がフォーカスの時点（とキーボードが出てくるあいだ）に読む**本物の入力欄の描かれた位置**は、はじめから最終位置（キーボードより上）でなければならない（2.16.2）。見た目だけを動かす。
- **いまの作り（installKeyboardInset の `prepLift`、`faEnd` から呼ぶ）**
  1. 紙の箱の位置を測る → レイアウトをその場で持ち上げる（2.16.2 のまま）。
  2. 紙の箱に FLIP：`translateY(元の位置との差)` → `0` を 280ms（`KB_ANIM_MS` / `KB_EASE`）。
  3. **本物の入力欄に、逆向きの同じ動き**（`translateY(−差)` → `0`、同じ時間・同じ曲線・同じ瞬間）。紙＋入力欄で毎コマ差し引き 0 になり、入力欄はずっと最終位置に描かれる。
  4. 本物は `.ft-kb-ghost`（文字・カーソル・枠・背景だけ透明）にし、見た目は紙の箱に足した複製（`data-ft-kb-clone`、読み取り専用・押せない）で見せる。
  5. 動き終わったら複製を外し、本物を見せる。位置は同じなので入れ替わりは見えない。focusout・もう一度の持ち上げ・下ろしでも片づける（`endLiftFx`）。
  - その後の focus は 2.16.2 と同じ（`focus({ preventScroll: true })`）。
- **やってはいけないこと**
  - ❌ **入力欄の逆向きの動きを外さない。** 外すと動いているあいだ入力欄がキーボードの裏に描かれ、iOS が画面ぜんたいを押し上げる。
  - ❌ **本物を `opacity: 0` / `visibility: hidden` で隠さない。** iOS が「見えない欄」とみなし、カーソルを出さない・フォーカスを外すことがある。`.ft-kb-ghost` の透明色で隠す。
  - ❌ 動きに fill（forwards / both）を付けない。
  - ❌ 持ち上げの動きを、FLIP 以外（padding の transition など）で作らない。
- **確かめたこと（Chromium、コマごと）**：本物の入力欄は focusin から終わりまで全コマ同じ位置（201px）。紙の箱は 544px → 107px を 280ms で動き、複製は紙といっしょに動いて最後に本物と重なって消える。**iPhone 実機では未確認。**
- **確かめ方（iPhone 実機、ホーム画面から開いたもの。入れ直してから）**：フォルダの設定で名前に触れる → 紙が下から滑らかに上がり、キーボードと並んで止まること。見出しがダイナミックアイランドの裏へ入らないこと（2.16.2 の成功が保たれている）。止まった直後にカーソルが出て、そのまま打てること。入れ替わりの瞬間に文字や枠がちらつかないこと。

### 入力欄に触れると、紙ごと画面が押し上げられて見出しがダイナミックアイランドの裏へ入る（2.16.2〜）　⚠️ 下の節（2.16.1）より優先

- **実機で起きたこと（2.16.1、ホーム画面から開いたもの）**：フォルダの設定で名前に触れた瞬間、紙の見出しと入力欄が画面の上へ押し上げられ、ダイナミックアイランドの裏へ入った。落ち着いたあとは、紙の下と帯（^ v ✓）のあいだから、暗くなっていない一覧がのぞいた。
- **画面写しを測って分かったこと**：押し上げの瞬間は、こちらが focusin で持ち上げたぶんと、iOS の押し上げ（約230px）が**両方**かかっていた。**iOS は、タップした時点の入力欄の位置で押し上げ量を決めている。** focusin で何をしても遅い。2.16.1 の「送り場の箱を縮める」も同じ理由で効かなかった。
- **いまの作り（installKeyboardInset）**
  - **こちらでフォーカスを入れる（本命）**：紙の入力欄、または全画面でキーボードに隠れる位置の入力欄をタップしたら、`touchend` で `preventDefault`。その場で場所を空け（紙を持ち上げる／送り場を縮めて中を送る）、入力欄がキーボードより上に来てから `focus({ preventScroll: true })`。iOS はタップで入力欄に入らないので、古い位置で押し上げ量を決めることがない。
    - すでに打っている入力欄のタップ（カーソル移動）と、隠れない位置の textarea のタップは iOS にまかせる。こちらで入れた input はカーソルを最後に置く。
  - **相殺（保険）**：それでも押し上げられたら（見込みより大きいキーボードなど）、`visualViewport.offsetTop` のぶん、いちばん外の層（`[data-ft-overlay]` / `.ft-sheet-wrap`）を CSS の `translate` で下へずらして見た目を戻す（`html[data-ft-comp]`）。紙の下の余白は、ずれを引かない高さ（`raw`）。
  - **レイアウトごと縮む iOS**：キーボードで見える高さといっしょにレイアウトも縮んだときは、こちらでは持ち上げない（二重になる）。
  - **ページの位置**：入力のあいだに iOS が送ったページは、キーボードが消えてから一度だけ `scrollTo` で戻す。
  - `.ft-sheet-wrap` にも `box-shadow: 0 0 0 100vmax #FFFFFF`（外がわだけ白）。
- **やってはいけないこと**
  - ❌ 紙の入力欄のタップを iOS のネイティブ（タップで入れる）に戻さない。
  - ❌ `touchend` 以外（`touchstart` / `touchmove`）で `preventDefault` しない。
  - ❌ 打っているあいだに `window.scrollTo` のくり返しで押し上げに対抗しない（2.16.0 までの揺れ）。
  - ❌ 相殺を `transform` で書かない（入場の anim-* や FLIP と取り合う）。`translate` プロパティで。
- **確かめ方（iPhone 実機、ホーム画面から開いたもの。入れ直してから）**：フォルダの設定で名前に触れる → 紙がその場で上がり、**見出し（フォルダの設定・✕）が1コマもダイナミックアイランドの裏へ入らない**こと。紙の下から一覧がのぞかないこと。Safari の「開発」で、打っているあいだ `visualViewport.offsetTop` が 0 のままか、`html` に `data-ft-comp` が付いていないかを見る（付いていれば、保険の相殺が働いた＝本命が効かなかった場面）。

### キーボードが出るとき、ヘッダーごと画面がずれる ── 「iPhone に画面を送らせない」形へ（2.16.1〜）　⚠️ 下の4つを戻さないこと

> 2.11.17（送り場の ::after 余白）・2.11.18（触れた時点で余白）・2.15.0（持ち上げの FLIP）・useLockBackground の visualViewport での引き戻しは、**この版で改めた。** 上の各節の該当部分より、この節が優先する。

- **前提（iOS の決まりごと。Safari もホーム画面のアプリも同じ）**
  - キーボードを出しても、**レイアウトの高さ（fixed の inset:0 / 100% / 100dvh / 100svh）は変わらない。** 変わるのは `visualViewport` だけ。ホーム画面のアプリにはブラウザの帯が無いので、dvh・svh・lvh はいつも同じ値。
  - `interactive-widget=resizes-content` は WebKit では効かない（Android の Chrome だけ）。index.html にも足さない。
  - iPhone は、入力欄（とカーソル）が「キーボードの上の見えている範囲」に無いと、**画面ぜんたい（見える窓）をずらして**見せる。fixed のヘッダーもいっしょにずれる。CSS（overflow / overscroll-behavior / position: fixed）ではこれを止められない。止められるのは「ずらす理由を無くす」ことだけ。
- **ずれていた理由（見立て）**
  1. 全画面の送り場は、下に ::after で余白を足すだけだった。送り場の**箱**はキーボードの裏まで伸びたままなので、iPhone から見ると入力欄は「送り場の中では見えている」。送り場は送られず、画面ぜんたいがずらされた。
  2. そのずれを `useLockBackground` の引き戻し（scroll と visualViewport の resize で `scrollTo`）が、キーボードの動きの最中に戻した。iPhone の送り（上）と引き戻し（下）が交互に描かれ、「一度下がって戻る」揺れになった。
  3. 紙の持ち上げの FLIP は、transform で紙を**元の位置から**滑らせていた。iPhone がキーボードを出しながら読むカーソルの位置は transform 込みなので、まだキーボードの裏にあることになり、画面ぜんたいをずらす理由になっていた。
- **いまの作り**
  - 全画面：打っているあいだ、中身の箱（`absolute inset-0 … flex-col`）に `padding-bottom: var(--ft-kb)`（`html[data-ft-kbctx="overlay"]`）。**送り場の箱そのものがキーボードの上端で終わる**ので、iPhone は送り場の中だけを送って入力欄を見せる。見出しは動かない。
  - 全画面も紙も、場所を空けるのは **focusin の中で、その場で**（触れた時点では空けない。箱が縮むと指の下から入力欄が消える）。
  - 紙の FLIP は**下ろすときだけ**。持ち上げはその場で一度。紙の下の白（::after）がキーボードとのすき間を埋める。
  - `useLockBackground` は、重なる層の中で打っているあいだは引き戻さない（`ftTypingInLayer()`）。visualViewport の resize でも引き戻さない。
  - 見込みが足りずに iPhone がずらしてしまったとき（はじめて・予測変換の帯・別のキーボード）は、`unpan` が**キーボードが落ち着いてから一度だけ**（resize / scroll が 160ms 来ない・指が触れていない）本当の高さで空け直し、留めた位置へ戻す。測った高さは覚えるので、2回め以降は起きない。
  - 全画面の入力欄から始まったタップの click が、縮んだあと指の下にある別のボタンへ届かないよう、`guardClick` を全画面にも広げた。
  - `[data-ft-overlay]` も `overflow: clip`（`.ft-sheet-wrap` の 2.16.0 と同じ理由）。重なる層の中の送り場は `overscroll-behavior: contain`。
- **やってはいけないこと**
  - ❌ 全画面の余白を送り場の ::after に戻さない。
  - ❌ 持ち上げ（kb が増えるとき）に transform のアニメーションを付けない。
  - ❌ 打っているあいだに、scroll / visualViewport のイベントの中で `scrollTo` しない（動いている最中に戻さない）。
  - ❌ `html` / `body` を `position: fixed` にして直そうとしない（2.11.16 のとおり、ホーム画面のアプリで fixed のものが約60px ずれる。しかも見える窓のずれは止まらない）。
- **確かめ方（iPhone 実機、ホーム画面から開いたもの。いったん入れ直す）**
  - Today を下まで送る → ＋ → メモ → 本文のいちばん下の行に触れる → **見出し（✕・種類・★）が1pxも動かず**、本文だけがキーボードの上へ送られること。
  - フォルダの追加・設定で名前に触れる → 紙がその場で上がり、キーボードが下から来て紙の下に付くこと。ヘッダー・うしろの画面は動かないこと。閉じると紙が滑らかに下りること。
  - はじめて開いたときに一度だけずれた場合も、キーボードが止まった直後に一度で戻り、2回めからはずれないこと。
  - Safari の「開発」から見るとき、`document.documentElement.dataset.ftKbctx`・`visualViewport.offsetTop`（打っているあいだ 0 のままなら成功）を見る。

### 検索で「見つかった場所」を見せる（2.11.14〜）　🔎 検索の対象を増やしたら、見せる側にも足すこと

- **計画**（`PlanScreen` の検索）は、計画名に加えて、中のイベントの**タイトル・チェックリストの項目・メモ**も対象。判定は `planHits(plan, q)` ひとつで行い、`matchPlan` もこれを使う（`PlanPickList` の計画さがしも同じ判定）。
- 検索中は `PlanCard` に `hitQ` を渡し、札の下に `PlanHitBox`（見つかった場所）を出す。イベントごとにまとめ、チェック・メモはそのイベントの下に寄せる（チェックは1イベント3件まで＋「ほか◯件」）。計画名に当たったときは名前そのものも塗る。
- **探す**（`FindScreen`）は、キーワードを入れて検索したときだけ `RecordRow` に `hits`（`recordHits(r, words)`）と `hitWords` を渡し、札のいちばん下に `RecordHitBox` を出す（5か所まで＋「ほか◯か所」）。長いメモは `hitSnippet` で当たった語のまわりだけを切り出す。
- 当たった語は `HitText` が `<mark class="ft-hit">` で塗る（GLOBAL_CSS、地は `--th-300`）。検索はどの記録にも共通の操作なので、**記録の色ではなく基調カラーを使う**（3章「色の3つの役割」の2）。
- ❌ **探す側の判定と見せる側の判定を別々に書かない。** 計画は `planHits`、記録は `recordAllText` と `recordHits` をそろえる。**`recordAllText` に項目を足したら `recordHits` にも足すこと。** 足し忘れると「検索に出るのに、見つかった場所が空」の札になる。
- ❌ **計画の読み込み（`normalizePlan` のイベントの写し）から `body` を落とさない。** 2.11.13 までここに無く、開き直すたびにイベントのメモが消えていた（検索にも出なかった）。イベントに項目を足すときは、ここにも一行ずつ足す。

---

### フォルダ一覧の2列カード（2.11.25〜）

`FolderScreen` の一覧は、縦1列の横長の札から、**2列のカード**に変えた（柱が 56rem になる 820px 以上では4列）。

- 並べ方・大きさは GLOBAL_CSS の `.ft-fgrid` / `.ft-fcard` / `.ft-fthumb` / `.ft-fpin` / `.ft-fbody` / `.ft-fname` / `.ft-fmeta` / `.ft-fsub` にまとめてある。
- **グリッドの箱に `space-y-*` を付けないこと。** 子の上に余白が付いて、段がずれる。すき間は `gap` で持つ。
- **絵の窓は `aspect-ratio: 1 / 1` のまま。** フォルダのアイコンは `CropSheet aspect:1` で正方形に切り抜いているので、窓の形を変えると絵が見切れる。
- **列の数を `auto-fill` で決めないこと。** 細い携帯（320px）で1列に落ちる。
- 「見つかりません」「まだフォルダはありません」は `.ft-noresult` を付けて、2列ぶんに広げている。

### 横長の画面で、2段の札の上がそろわない（2.12.0〜）

- **起きていたこと**：iPad などを横にすると、計画の一覧の右の段のいちばん上の札が、左の段より 10px 下がっていた。
- **原因**：横長では `.ft-spread` が中身を `column-count: 2` で2段に流し込む。計画の一覧は `space-y-2.5`（2枚目から上に余白）で間をあけていたため、右の段の先頭に来た札にもその余白が残った（WebKit は段の切れ目で余白を消さない）。記録の一覧は `ft-slot` の下の余白で間をあけているので起きていなかった。
- **直し方**：2段のとき、`space-y-*` の付いた `.ft-spread` だけ、子の余白を上から下へ移した（`margin-top: 0; margin-bottom: 16px`）。下の余白なら、段の切れ目では左の段の底に隠れる。
- ❌ **`.ft-spread` の中の間隔を、上の余白（`space-y-*` / `mt-*`）で作らないこと。** 下の余白（`ft-slot` など）で作る。

**2.19.0〜　計画の一覧だけは、段組みをやめて格子（`.ft-pgrid`）にした。**
- 上の直し（2.11.27）のあとも、iPhone を横にすると、計画の一覧の**右の段の先頭の札が 16px 下がる**のが残った（保育士試験｜やりたいこと の2枚だけのときに確認）。段組み（`column-count`）の切れ目の余白の扱いに、WebKit の癖が残る。
- `PlanScreen` の一覧は `ft-spread` をやめ、`ft-pgrid`（横長のときだけ `display: grid` の2列、`gap: 16px`、`align-items: start`）にした。どの札も行の上端から始まるので、上が必ずそろう。縦向きは今までどおり `space-y-2.5` の1列。
- **並びは左から右、上から下**（1枚目｜2枚目、3枚目｜4枚目）。段組みのときの「左の段を上から下へ、次に右の段」とは順序が変わる。
- ❌ **計画の一覧に `ft-spread` を戻さないこと。** 記録の一覧のように高さがまちまちで、つめて並べたい所（`ft-slot`）だけが段組みの対象。
- 格子は「同じ行の中で高い札にそろえて行が伸びる」ので、高さのちがう札が並ぶと、低い札の下に少し空きができる。これは意図した見え方（上をそろえるほうを優先）。
- **この直しは、実機の iPhone（WebKit）では確かめていない。** 作業環境には Chromium しかなく、段組みの癖そのものは再現できなかった。格子は書いたとおりに置くので、癖の出る余地が無い、という理由で選んだ。実機で右の札の上がそろっているか、必ず確かめること。

### フォルダの並び順に「件数順」を足す・フォルダと計画で並び順を分ける（2.13.0〜）

- **フォルダだけ「件数順」（中に集まる記録が多い順）を選べるようにした。** 計画には足していない（計画の「件数」に当たるものが無い）。
- **フォルダと計画は、それぞれ別のデフォルトの並び順を持つ。** 以前は表示設定の「フォルダ・計画」ひとつを2つのタブで共用していた。
- **`SortToggle` は `options` を渡せる部品にした。** 渡さなければこれまでどおり名前順／更新順の2つを押すたびに入れ替える。フォルダは `FOLDER_SORT_OPTIONS`（名前順／更新順／件数順の3つ）を渡し、押すたびにこの3つを順ぐりに回る。**新しい並び順を足すときは、ここに `{ value, label }` を足すだけでよい。** 個別に分岐を書き足さないこと。
- **並べかえそのもの（`sortItems`）も、`counts`（id → 件数 の Map）を渡せば「件数順」で並べられる。** 件数が同じときは名前順で決める（**無音順（データの並びのまま）にしないこと**。開くたびに順番が変わって見える）。
- **件数は `FolderScreen` の中で一度だけ数え、並べかえとカードの表示の両方に使う。** カードごとに `folderRecords` を呼び直さない（フォルダの数だけ記録をなめる計算なので、二重にしない）。
- **保存する場所（`prefs`）**：`sortOrder` は計画専用のまま名前を変えていない。フォルダは新しく `folderSortOrder` を持つ。
  - **古い設定（`folderSortOrder` が無い）を読み込んだときは、それまで共用していた `sortOrder` の値を引き継ぐ。** 引き継がないと、更新したとたんフォルダの並びだけ「名前順」に戻って見える。`loadPrefs` と、バックアップからの読み込み（`restore`）の両方に、この引き継ぎを入れてある。**並び順にかかわる保存の入り口を増やしたら、ここにも同じ引き継ぎを足すこと。**
  - `folderSortOrder` も `sortOrder` と同じく、並び順は「見かたの好み」なのでバックアップを促す数には数えない（`PREF_NOT_CONTENT` に足してある。6-⑦を参照）。
- ❌ **フォルダと計画のどちらか一方だけ直して、もう一方の呼び名（`draft.sortOrder` と `draft.folderSortOrder`）を書き間違えない。** 表示設定の「フォルダ」の行は `folderSortOrder`、「計画」の行は `sortOrder` を見る。

## 4. 部品を作るときに、まず探すべき既存パーツ

新しく書く前に、まずこれらで足りないか確認してください。車輪の再発明が一番の「統一感の崩れ」の原因です。

| やりたいこと | 使うもの |
|---|---|
| ボタン | `BTN_PRIMARY` / `BTN_SECONDARY` / `BTN_DANGER` / `BTN_DANGER_SOFT`（`BTN_BASE` に色だけ足す形） |
| ボタンの高さ | `.btn-h`（44px）/ `.btn-h-lg`（48px）※ 値の定義は app.js 側にある |
| 記録カードの並び | `CARD_LIST` ＋ 個々を `CARD_SLOT` で包む |
| 折りたたみ（開く・たたむ） | `Collapse`（`open` / `keepMounted`）。矢じりには `ft-chev` を付ける（2.18.0〜） |
| 同じ場所の出し分けタブ（進行中／完了済など） | `PillTabs`（`tabs` / `value` / `onChange` / `onCanvas`）（2.18.0〜） |
| 白い札を並べる画面の下地 | `.bg-canvas`（`--color-canvas`）。札は白のまま（2.18.0〜） |
| 設定画面の1行 | `SheetRow`（高さ固定 `min-h-[58px]`、補助テキストの有無で高さがブレない） |
| 複数の設定行を白カードでまとめる | `GroupCard`（1項目だけのときは使わないこと、という決まりが元コードにあり） |
| **押すとひと呼吸おいて画面が変わるボタン**（戻る・キャンセル・メニューの行） | `TapButton`（中身は `useTapThen`） |
| 一覧の並べかえ（押すたびに順ぐりに変わる） | `SortToggle`（`options` に `{ value, label }` の配列を渡す。渡さなければ名前順／更新順の2つ） |
| **印の入り切り**（チェック・固定・やり終えた） | `TapOnceButton` / `useTapOnce` |
| 押した感触（沈み込み） | `.ft-tap`。カードには `.ft-tap-card`、アイコンには `.ft-tap-icon` を追加で付ける（沈み込み量が場面ごとに使い分けてあるので、そのまま使う） |

> 🔴 **`TapButton` と `TapOnceButton` を取り違えないこと。ここが実際に事故になりました。**
>
> 以前の引継書には「タップ時に少し待ってから実行するボタン → `TapButton`」とだけ書いてありました。
> **この説明のまま印のボタンに使うと、連打がそのまま抜けます。**
> `TapButton` の中身（`useTapThen`）は、押したあと60ms待ってから動き、
> **待っているあいだに来た2度目を捨てる**作りです。画面が切り替わるボタンなら
> 「二重に開かない」ための正しい守りですが、印の入り切りに使うと
> 「ゆっくり押せば入るのに、速く押すと入らない」＝先祖返りに見える不具合になります。
>
> - 画面が切り替わる → `TapButton`
> - 値が変わるだけ（印を付ける・外す） → `TapOnceButton`
>
> 名前が似ているので、新しく部品を足すときは `grep -n "function 名前"` で
> 既にないか必ず確かめること（1章の警告と同じ理由です）。

> ⚠️ **`.ft-tap-card` の中に押せるものを入れるとき**
> `:active` は押した要素だけでなく**先祖の要素にも伝わります。** そのままだと、
> カードの中のチェックを押すたびに外側の札ぜんたいが沈み、点滅して見えます。
> `app.css` の `@supports selector(:has(*))` の打ち消しでこれを止めてあります。
> **この打ち消しを消さないこと。** 沈むのは、いま指が乗っているものだけでよい。

### リンクの札・写真の札を押すと、記録カードごと濃く沈む（2.11.6〜）

- **起きていたこと**：リンクの札（`LinkCard`。YouTube のサムネイルや「X」「Google マップ」などの札）や、記録内の写真のサムネイルを押すと、その札だけでなく、くるんでいる記録カード（`RecordRow` の `<article>`）まで沈んで見えた。大きな札では記録カードのほとんどが沈むので、「画面全体が暗くなった」ように見えていた。
- **原因**：`.ft-tap-card` どうしの入れ子（リンクの札・写真のサムネイルは、それ自体も `.ft-tap-card`）。**`:active` は先祖にも伝わる**（1章・4章と同じ理由）。以前の `:has()` の打ち消しは、中の押せるものが `.ft-tap` か `.ft-tap-icon` のときだけを見ていて、中身が **`.ft-tap-card`** そのもの（リンクの札・写真の札）のときは抜けていた。
- **決まり**：`@supports selector(:has(*))` の打ち消しに `:has(.ft-tap-card:active)` を足してある。**新しく「記録カードの中に、それ自体が `.ft-tap-card` の札」を置いたら、この打ち消しの対象に入っているか確かめること。**
- あわせて、記録カードを押したときの沈み（`.ft-tap-card:active::after` の `opacity`）を `.07` から `.045` に下げた。**濃さを変えるときは、この一箇所だけを直せばよい。**

### 写真のある記録の上から画面を送る（2.11.5〜）　⚠️ 札に transform / filter / mix-blend-mode を戻さないこと

- **起きていたこと**：フォルダの中で、写真を付けた記録（YouTube のサムネイル付きの札も同じ）の上から上下に送ろうとすると、画面が送られなかった。2.11.4 では URL が原因と見て直したが、**原因ではなかった**（下の 2.11.4 の手当ては害がないので残してある）。
- **起きる画面**：画面ぜんたいではなく「箱の中で送る」画面（`OverlayScreen` の中の `overflow-y-auto`）。フォルダ・計画・日の画面・記録の入力画面・記録を入れるシートなど。Today のような画面ぜんたいの送りでは起きない。
- **原因（見立て）**：押した瞬間に札へ `transform: scale()` と `filter: brightness()`（`.ft-tap.ft-tap-card:active` / `.ft-pressing`）が当たり、さらに `::after` の `mix-blend-mode: multiply` があるため、iPhone が札と中の写真を別の層に持ち上げる。その層の上で動かした指が、送る箱に届かない。
- **決まり**
  - `.ft-tap-card` 系（`:active` / `.ft-pressing` / `.ft-tap-pressed`）には transform も filter も当てない。手ごたえは `::after` の膜だけ。
  - `.ft-tap-card::after` / `.ft-tap-icon::after` に `mix-blend-mode` を付けない。
  - `.ft-root button img, .ft-root a img` は `pointer-events: none`（指を受けるのは外側のボタン）。
  - 確かめは iPhone 実機で、写真を付けた記録を 3 件以上入れたフォルダを開き、写真の上から送って行う。

### 重ねるボタンが消える（2.11.5〜）　⚠️ `.ft-tap-card` / `.ft-tap-icon` の relative で .absolute を消さないこと

- app.css の `.ft-tap-card, .ft-tap-icon { position: relative }` は Tailwind の `.absolute` より後ろにあるため、`absolute ... ft-tap-icon` と書いたボタンが relative に戻されていた。記録の入力画面の「写真の右上の✕」が写真の下へ押し出され、切られて見えなかった。
- いまは `:not(.absolute):not(.fixed):not(.sticky)` を付けてある。**外さないこと。**
- 写真の✕は `TapOnceButton` で受ける（値が変わるだけのボタン）。

### リンク（URL）の上から画面を送る（2.11.4〜）　⚠️ リンクを「つかめる」ままに戻さないこと

- **起きていたこと**：URL を含む記録（本文のリンク・リンクの札・場所のリンク）の上に指を置いて上下に送ろうとすると、画面が送られず固まったように見えた。字の上からなら送れる。
- **原因**：iPhone は、リンクや絵に指を置いて少しでも止まると「つまんで運ぶ（ドラッグ）」や「リンクのプレビュー」を始め、指の動きをそちらに取られる。
- **決まり**
  - `.ft-link` には `-webkit-user-drag: none` / `-webkit-touch-callout: none` / `user-select: none` を付けてある（GLOBAL_CSS）。`.ft-text` の中でも効くよう `.ft-text .ft-link` の形でも書いてある。**新しくリンクを置くときは、かならず `ft-link` を付け、`draggable: false` を渡すこと。**
  - 札の中の絵（`.ft-link img`）は `pointer-events: none`。指を受けるのは外側のリンクだけ。
  - `.ft-root img` にも `-webkit-user-drag: none`。`Photo` は `draggable: false` を出す。
  - 「押した」と「送った」の見分けは、document に付けた `linkClickGuard` が行う。指を置いてから 10px を超えて動いた、または指を置いているあいだにどこかが送られたら、そのあとの `click` を捨てる。**touchstart / touchmove は passive のまま、preventDefault を呼ばないこと**（呼ぶと画面が送れなくなる）。

---

## 5. 新しい画面を作るときのチェックリスト

- [ ] 文字サイズは `.fs-*` のいずれかを使っている（新しい生の `text-[Npx]` を作っていない）
- [ ] 太字にしたい場合は `font-bold` を別途付けている（`.fs-*` 自体に太さは無い）
- [ ] 新しいトークンを追加した場合、小・中・大を実際に切り替えて確認した
- [ ] 余白は 2/4/6/8/10/12/16/20/24/28/32px のいずれか
- [ ] **記録カードの内側は `px-4 py-3.5` を外の箱ひとつで持たせた**（3章「記録カードの中の余白」）
- [ ] **新しく書いた余白・大きさのクラスが app.css（または GLOBAL_CSS）に本当にあるか `grep` した**（無いクラスは黙って効かない）
- [ ] アイコンは3章の対応表にあるサイズか、無ければ表に追記した
- [ ] 色は「記録固有」「アプリ基調」「状態色」のどれかを判断して、該当する仕組みを使った
- [ ] 新しいアニメーションを作る前に、既存のキーフレーム・イージングトークンで足りないか確認した
- [ ] **画面・シート・小窓の入ってくる向きを、3章「画面遷移の決まり」の表で決めた**（階層＝右、入力・独立した作業＝下、シート＝下、小窓＝その場で薄く、同じ階層＝動かさない）
- [ ] **全画面は `OverlayScreen` を通し、`from` を渡した。** 右からの画面には `useEdgeSwipeBack` をつなぎ、`goFromMenu` にも閉じる行を足した
- [ ] **下タブの画面なら、もう一度押したとき「1回め＝いまの表示の上」→「2回め＝デフォルトの表示」に戻るよう `useTabReturn` をつなぎ、`AppMain` から `tabReset` を渡した**（3章「下タブをもう一度押したときの戻り方」の表にも1行足した）
- [ ] **画面の中の部品（アイコン・文字・札）に、出てくる動きや飾りの動きを付けていない**
- [ ] **上に留める見出し・帯を `position: sticky` にしていない**（画面ぜんたいを送る場所は `TopChrome`、送る箱の中なら箱の外へ出す。3章「画面の上に留める見出しと帯」）。確かめは iPhone 実機で、いちばん下まで送って行う
- [ ] **保存する中身を増やしたら、書きかえを `noteChanges` に渡した**（渡さないと、バックアップを促す数から漏れる。6-⑦）
- [ ] **入力欄のある紙・小窓は `.ft-sheet-wrap` で作った**（キーボードの上へ持ち上げるのはここだけに効く。3章「下から出る紙の入力欄が、キーボードの裏に隠れる」）
- [ ] **紙・小窓・メニューは、うしろを留める仕組み（`useLockBackground` か `BackgroundLock`）を通した**（通さないと、iPhone でキーボードを出したときにいちばん下の一覧が透ける。3章「紙・小窓を開くと、いちばん下の一覧が透ける」）
- [ ] **退場の動きを、登場と対にした**（右→右、下→下、その場→その場）。長さは `FT_EXIT_*_MS` と `--ft-enter-*` でそろえた（3章「画面遷移の決まり」）
- [ ] **✕・キャンセル・背景だけでなく、「保存」「決定」「選ぶ」も `requestClose` に通した**（通し忘れると、そのボタンだけパッと消える）
- [ ] **紙・小窓の中の字を上下に払っても、紙もうしろの一覧も動かない**（6-⑪。実機で確かめること）
- [ ] **折りたたみは `Collapse` を通した**（`open && …` や `hidden` の切り替えで書いていない。3章「折りたたみ」）
- [ ] **進行中／完了済のような出し分けは `PillTabs` で作った**（灰の下地の上なら `onCanvas`）
- [ ] 「画面の動き」をオフにして、すぐ定位置に出ることを確かめた
- [ ] カードの余白は、日付など要素の有無で崩れていない（`RecordRow` や `SheetRow` のように高さ・余白を固定して吸収する）
- [ ] **新しい部品の名前が、既にある部品と重なっていないか `grep` した**
- [ ] **リンクには `ft-link` と `draggable: false` を付けた**（付けないと、その上から画面を送れなくなる）
- [ ] **印の入り切りのボタンは `TapOnceButton` を通した**（`TapButton` ではない）
- [ ] **検索の対象を増やしたら、`planHits` / `recordHits`（見つかった場所）にも足した**
- [ ] app.css を変更したら app.js 側に同名クラスの重複定義が無いか確認した
- [ ] **app.css / app.js を変更したら、index.html の `?v=` と `window.__MT_VERSION` を上げた**（上げないと端末に古いファイルが残り、直したはずの見た目や動きが反映されない）

---
---

# 第2部　保存とタップ ── 「先祖返り」を二度と起こさないために

チェックリストの印が、操作したあと（多くは開き直したあと）古い状態に戻る。
「画面には入っているのに、保存された中身だけ古い」という形をとることもある。

**同じ不具合を三度くり返しました。** 原因は一つではなく四つの層に分かれていて、
一つずつ直すたびに「直った」と思い、残りの層でまた戻る、ということを繰り返しています。
**どれか一つでも外すと、また戻ります。**

## 6-①　置き場（app.js「保存まわり」）

- 同じキーへの書き込みは、かならず `storageSet` を通す。`window.storage.set` や `localStorage.setItem` を直に呼ぶ道を作らない。
- `storageSet` はキーごとに順番待ちの列を作り、待っているあいだに新しい値が来たら古いほうを捨てて最新だけを書く。**この列を迂回する近道を作らないこと。**
- 置き場はふたつある（専用ストレージ＝`window.storage` と、控え＝`localStorage`）。専用ストレージにだけ書けなかったときは控えのほうが新しくなるので、`key::localnewer` という印を立て、読む側（`storageGet`）がその印を見る。**二段構えを増やすときは、かならず「どちらが新しいか」を決める道をいっしょに作ること。** 印がないと、次に開いたとき専用ストレージの古い中身がそのまま読まれる。
- 書いたあと600msで読み返し、食い違っていたら書き直す（`scheduleVerify`、最大2回）。どの層で取りこぼしても最終的に画面と保存がそろう、最後の歯止め。**外さないこと。** 書き直しが走ると `console.warn("保存を読み返したら食い違っていたので、書き直します")` が出る。

**なぜ**：`window.storage` は非同期で、遅延にばらつきがある。先に投げた書き込みがあとに着地することがあり、画面は最新なのに置き場だけ古い中身で塗り替えられる。

## 6-②　状態（app.js `AppMain` の `setRecords` ほか）

**保存を伴う書きかえは、かならず「ひとつ前をもらって次を返す」形で書く。**

```js
setRecords((prev) => prev.map(...));   // ○
setRecords(records.map(...));          // × いま描かれている一覧を丸ごと写している
```

- `records` / `plans` / `folders` / `kinds` / `tagMaster` / `prefs` は、それぞれ覚え（ref）を持っている。覚えは押したその場で進み、画面はあとから追いつく。配列やオブジェクトを丸ごと渡すと、`resolveNext` が覚えごと古い値で上書きしてしまう。
- **描き直しのたびに覚えを書き戻さないこと。** 覚えを書き替えるのは、読み込みのときと、各 setter の入り口の2か所だけ。
- とくに `await` をまたぐ処理で、`await` より前に読んだ一覧を使わない。`saveRecord` は写真の退避（`stashPhotos`）で待つので、その間に付けられたチェックが、保存の一行で丸ごと巻き戻る。
- 各 setter は入れた値を返す。片づけ（`sweepPhotos`）などで「いまの一覧」が要るときは、render スコープから拾い直さず、この戻り値か ref を使う。

## 6-③　タップの受け口（`useTapOnce` / `TapOnceButton`）

- **印の入り切りを `onClick` だけで受けない。** `useTapOnce`（または `TapOnceButton`）を通す。
- **`TapButton`（`useTapThen`）を印のボタンに使わない。** 第1部4章の赤枠を参照。

**なぜ**：iOS では素早く連続して押すと、2回目以降を「ダブルタップの一部」とみなして `click` を配らないことがある。ゆっくり押したときだけ正しく入り、速く押したときにだけ抜ける、という形になる。`useTapOnce` は指を離した時点（`pointerup`）で受け止め、あとから来る `click` は捨てる。押さえたまま滑らせて逃げたぶん（12px超）は受けない。キーボードの Enter / Space から来る `click` は時間で見分けて通す。

**⚠️ 「もう受けた」の記録は、部品ごとではなくアプリ全体で持つ（2.11.19〜）**　`tapOnceGlobal.at`（指を離した時刻）

- 症状：写真が2枚のとき2枚めの✕を押すと、残った1枚も消える（`ImagesField`）。
- 原因：iPhone は指を離して0.3秒ほど遅れて `click` を配る。その間に画面の形が変わると、`click` は「指を離した場所に、いまあるもの」へ届く。2枚→1枚で残った写真が2列から1列の大きな表示に描き直され、その✕がちょうど指の下に来て、遅れた `click` を受けてしまった。
- なぜ前の仕組みで防げなかったか：「もう受けた」の記録（`st.current.at`）が部品ごとだったため、押した✕しか見張れず、別の✕が受け取る道は素通りだった。
- 直し方：`useTapOnce` の `pointerup` で、指を離した時刻をモジュール全体の `tapOnceGlobal.at` にも残し、`onClick` では**自分の記録と全体の記録の新しいほう**から 700ms 以内のものを捨てる。
- **`onClick` の判定を、`st.current.at` だけに戻さないこと。** 消える／並びが変わる／大きさが変わる部品の中の `TapOnceButton`（写真の✕・行の削除など）で、同じ事故が戻る。
- 確かめ方：写真を2枚入れて2枚めの✕を押し、1枚だけ残ること（1枚めの✕でも同じ）。3枚→2枚、4枚→3枚も見る。

### 写真の欄（`ImagesField`）の見た目（2.11.19〜）　⚠️ 枚数で並びを変えないこと

- **1枚も無いときに、点線の大きな箱を出さない。** はじめは「写真を追加（あと4枚）」のボタン1本だけ。1枚あるときの追加ボタンと同じ見た目・同じ余白にそろえてある。
- **1枚のときだけ大きな1列（`grid-cols-1` / `4 / 3`）にしない。** 枚数にかかわらず、いつも2列・正方形（`grid-cols-2` / `1 / 1`）。枚数で大きさが変わると、✕で外した瞬間に残った写真が描き直され、遅れて届く押し込みが別の✕に当たる（6-③ の事故そのもの）。
- **「絵は4枚までです」のお知らせは出さない。** ボタンに「あと○枚」「写真は4枚まで」と出ているので、同じことを二度言わない（入りきらないぶんは黙って落とす）。読み込めない画像のお知らせは残してある。

## 6-④　版数（index.html）

**版数は `X.Y.Z`（メジャー.マイナー.パッチ）の形で持つ（2.12.0〜）。** どの桁を上げるかは、その回に入れる変更のうち**いちばん大きいもの**で決める。

| 桁 | 呼び名 | 上げる基準 |
|---|---|---|
| X | メジャー | 見た目や操作性に影響を及ぼすような大きな変更、ページの追加など |
| Y | マイナー | 細かな機能向上、部分的な情報の追加、ページの修正など |
| Z | パッチ | バグ修正、誤字脱字の訂正など |

- **1回の書き出しに複数の変更が混ざるときは、いちばん大きい基準の桁だけを1つ上げる。** それより下の桁は 0 に戻す（Y を上げたら Z は 0。X を上げたら Y も Z も 0）。バグ修正だけの回は、これまでどおり Z だけを上げる。
- 迷ったら「使う人の目に見える変わり方の大きさ」で判断する。たとえば：
  - 一覧の並べ方を丸ごと作り替える（縦1列 → グリッドなど）、新しい画面を足す → **X寄り**
  - すでにある仕組み（バックアップを促す、検索の対象など）が扱う範囲を広げる → **Y**
  - 見た目や扱う範囲は変えず、隠れていた不具合（紙がキーボードに隠れる、段がずれるなど）を直す → **Z**
- 1つの回に大きさの違う変更が混ざったとき（例：バグ修正と、既存の仕組みの対象を広げる変更を同じ回で出す）は、**混ざった中でいちばん大きい桁を上げる**（バグ修正＋機能の対象拡大 → Y を上げる）。
- ※ この決まりを作る前の版（〜2.11.27）は、さかのぼって振り直していない。これから先の版に適用する。

**`window.__MT_VERSION` と `app.js?v=` / `app.css?v=` を必ずそろえる。** 中身を直したら、かならず三つとも上げる。

**なぜ**：そろえ忘れると端末が古い本体を読み続け、直したはずの不具合が「再発した」ように見える。実際、一度ここでつまずいた（本体は 2.9.0、`?v=` は 2.8.0）。第1部5章のチェックリストにも前から書いてあったのに、それでもずれました。実機で確かめるときは、ホーム画面のアプリをいったん削除して入れ直すと確実。

## 6-⑤　ヘッダーの写真・フォルダのアイコン（2.10.2〜）

「記録の写真は残るのに、ヘッダーとフォルダのアイコンだけ、更新のたびに消える」を直したときの決まり。

**しくみの違い**（ここが原因だった）

| | 記録の写真 | ヘッダー・アイコン |
|---|---|---|
| 絵を置き場（IndexedDB）へ書く時 | 「保存」を押した時（`stashPhotos`） | 切り抜いた時（`photoPut`） |
| 参照（`photo:番号`）を書く先 | 記録の一覧（しょっちゅう書き直す） | 表示設定・フォルダ（めったに書き直さない） |
| 片づけ（`sweepPhotos`）で使用中と数えられた版 | 最初から | あとの版から |

- 置き場（IndexedDB）は版をまたいで共通。更新の直後に端末に残った**古い本体**が片づけを一度でも走らせると、ヘッダーとアイコンの絵だけが消える。新しい版の片づけを直しても防げない。
- 表示設定・フォルダの保存が専用ストレージにだけ入らなかったとき、印（`::localnewer`）が立って控えを読むので、読み返しは「一致」と判定され、専用ストレージは古いまま残っていた。記録は次のチェックで丸ごと書き直されて自然に戻るが、表示設定は戻る機会がない。

**決まり**

- ヘッダー・アイコンの絵は、置き場に加えて `hibi-decophotos`（専用ストレージ＋控え）にも写す。`savePrefs` / `setFolders` / `setPlans` と読み込みの直後に `scheduleDecoSync` を呼ぶ。**新しく「表示設定・フォルダ・計画に絵を持たせる」道を作ったら、その setter を通すこと。**
- `photoGet` は置き場に無いとき `hibi-decophotos` を見て、見つかれば置き場へ戻す。
- `sweepPhotos` は `hibi-decophotos` にある絵を消さない。
- 控えを作り直すとき、手元に無い絵があっても**止めない**（止めると、あとから設定した絵の控えも作られない）。
- 読み返し（`scheduleVerify`）は、控えと一致しても印が立っていれば専用ストレージへ入れ直す。起ち上がりと画面に戻ったときも `resyncLocalNewer(SYNC_KEYS)` で入れ直す。**保存するキーを増やしたら `SYNC_KEYS` にも足すこと。**
- `photoDB` は `PHOTO_DB_WAIT_MS`（4秒）で待つのをやめる。iPhone では更新直後に `indexedDB.open` が返事をしないことがある。あとから開けたら `notifyPhotoStore` で絵の部品に知らせ、読み直させる。
- すでに消えてしまった絵は戻せない。設定し直すか、写真入りのバックアップから戻す。

---

## 6-⑦　バックアップを促す数（2.12.0〜）　⚠️ 保存する中身を増やしたら、控えに数えること

メニューの三本線の丸・Today の「書き出していない変更があります」・バックアップ画面の「○件書きかえ」は、同じ数（`unsavedCount`）を出している。

- **以前の数え方の漏れ**：記録の時刻（`updatedAt` / `checkedAt` / `createdAt`）が前回の書き出しより新しい記録だけを数えていた。そのため次のものは数に出なかった（丸は出ても数字が空、または丸も出なかった）。
  - 消した記録（もう一覧に無いので時刻を見られない）
  - フォルダの名前・アイコン・条件、計画の名前・アイコン・イベント
  - 表示設定（色・文字の大きさ・動き・ヘッダーの写真・呼び名など）、タグの追加・名前の変更・並べ替え・削除（`setTagMaster` と `savePrefs` は、そもそも「書きかえあり」を立てていなかった）
- **いまの形**：
  - `setRecords` / `setPlans` / `setKinds` / `setFolders` は、前後の一覧を `changedIds` でくらべ、増えた・消えた・中身が変わったものを `rec:id` / `plan:id` / `kind:id` / `folder:id` として `noteChanges` に渡す。
  - `setTagMaster` は中身が変わったら `tags`、`savePrefs` は並び順（`sortOrder` / `folderSortOrder` / `recordOrder`）と `lastBackup` 以外が変わったら `prefs` を渡す（`PREF_NOT_CONTENT`）。
  - `noteChanges` は控え（`CHANGE_LOG_KEY` = `hibi-changelog`、`{ キー: 時刻 }`）に書き、`bumpChanged` を呼ぶ。数は「記録の時刻で新しい記録」と「控えのうち前回の書き出しより新しいもの」を集合で合わせて数える（同じ記録を二度数えない）。
  - 書き出したときと、読み込んだときは、控えを空に戻す。
- **やってはいけないこと**
  - ❌ **保存する中身（キー）を増やしたとき、`noteChanges` を通さずに保存しない。** 書き出しを促す数から漏れる。`SYNC_KEYS` と同じく、ここも足す場所として覚えておく。
  - ❌ **並び順の切り替えのような「見かたの好み」を数えない。** 一覧の上のボタンを押すたびに書き出しを促すことになる。数えないものは `PREF_NOT_CONTENT` に足す。
  - ❌ **中身が同じなのに数えない。** `changedIds` は参照が同じならくらべず、違えば中身（JSON）でくらべる。全件を `map((r) => ({ ...r }))` で作り直しても、中身が同じなら数えない。
- **確かめ方**：バックアップを書き出す → フォルダの名前を変える → 三本線の丸に「1」が出ること。続けて記録をひとつ消す →「2」、表示設定で色を変えて保存 →「3」。一覧の並び順を切り替えても数が増えないこと。もう一度書き出すと丸が消えること。

## 6-⑥　書きかけ（自動下書き）（2.11.12〜）　⚠️ 消す・閉じるときに端末の書きかけを残さないこと

- **起きていたこと**：記録を削除したあと次に開くと、消したはずの記録が「書きかけの記録があります」に出てきた。続きを開いても、入力画面に「削除」が無く、消す手だてが無かった。
- **原因**
  1. 入力画面（`RecordForm`）は書き直すたびに 0.8 秒後、端末へ書きかけ（`DRAFT_KEY`）を書く。**保存（`saveRecord`）では消していたが、削除（`deleteRecord` / `deleteMany`）と「保存せずに閉じる」では消していなかった。** 端末に残った書きかけが、次の起動で出ていた。
  2. 削除ボタンは「一覧にその記録があるときだけ」出していた。消したあとの書きかけや、まだ一度も保存していない書きかけは一覧に無いので、ボタンが出なかった。
  3. ついでに見つけたこと：`saveRecord` の `exists` に定義が無く、保存のたびに `ReferenceError` で止まり、「記録しました／書き直しました」が一度も出ていなかった（2.11.10 まで）。
- **いまの作り**
  - `AppMain` の `draftIdRef` が、端末に置いてある書きかけの記録 id を持つ。**画面の `draft`（state）で判断しないこと。** 続きを開いた時点で state は null になるが、端末にはまだ残っている。
  - `clearDraft()`（まるごと消す）／`clearDraftOf(ids)`（その記録の書きかけのときだけ消す）。保存・削除・まとめて削除・「保存せずに閉じる」から呼ぶ。
  - `RecordForm` は、削除（`__all` / この日より先 / ふつうの削除）の前に `doneRef` を立てる。
  - 書きかけには `__existed`（保存ずみの記録を書き直していた）を付けて置く。起動時、`__existed`（または以前の版で残った `updatedAt` 付き）なのに一覧にその記録が無ければ、消したあとと見なして黙って捨てる。
  - 書きかけから開いたときは `__fromDraft` を付け、一覧に無くても「削除」を出す（押すと書きかけを捨てる）。`__fromDraft` / `__existed` は保存の前に外す。
  - 一覧に無い記録を「削除」したときは、一覧を書かない（「まだ控えを取っていない」の数を増やさない）。
- **やってはいけないこと**
  - ❌ **記録を消す・閉じる新しい道を足したら、`clearDraftOf` を通すこと。** 通さないと、消したものが書きかけとして戻ってくる。
  - ❌ **削除ボタンの条件を「一覧にあるときだけ」に戻さない。**
  - ❌ **`saveRecord` の `exists` を消さない。**
- **確かめ方**：記録を開いて書き直し、1秒待ってから削除 → アプリを開き直す → 「書きかけ」が出ないこと。新しいメモを書いて1秒待ち、開き直す → 「書きかけ」が出る → 続きを開く → 「削除」がある → 消す → 開き直しても出ないこと。

## 6-⑧　途中保存と、星（印）の廃止（2.17.0〜）

**途中保存（`RecordForm` 右上）**

- 右上のボタンは「途中保存」。保存して、**画面は閉じない**。下の「保存」は保存して閉じる。
- 中身は `saveRecord(rec, { keepOpen: true })`（同じ id を上書きするので、何度押しても記録は増えない）。絵は `SaveArrowIcon`（保存する）→ `SaveCheckIcon`（保存できた）に約1.6秒変わる。
- ❌ **途中保存で `setEditing` を呼ばない。** 入力欄が作り直され、打っている最中のカーソルが外れる。`keepOpen` の枝は呼ばない作りになっている。
- 途中保存すると `dirty` を戻す（保存ずみなので、そのまま閉じても「保存せずに閉じますか」を出さない）。書き足せば `set()` がまた立てる。
- 保存ずみの中身と同じ端末の書きかけ（6-⑥）は、途中保存のときに消す（`saveRecord` の `keepOpen` の枝で `clearDraftOf`）。
- 確かめ方：新しいメモを書いて「途中保存」→ 画面は開いたまま、カーソルが外れない。閉じて Today に記録があること。続けて書き足して「途中保存」を何度か押しても記録が増えないこと。

**星（印）の廃止**

- 記録の `mark`、フォルダの `marked`、探す・フォルダ条件の「印つき」、`MARKS` / `markOf` / `MarkDot` / `MarkPicker` をすべて削除した。
- 古い記録・バックアップに `mark` / `marked` が残っていても、`migrateRecord` / `migrateFolder` が読み込むときに落とす（`undefined` を入れる。`excluded` と同じ書き方）。
- ❌ **「印つき」をふたたび足さない。** 大事なものを上に置く用途は「固定（`pinned`）」がある。

## 6-⑨　重なって出る窓は、指をうしろへ通さない（2.17.1〜）　⚠️ 札の中に作る窓・紙は特に

- **起きていたこと**：記録の絵を開いて（`PhotoViewer`）横に払って次の絵へ移すと、うしろの Today の日付（日・週・月）まで動き、窓ごと消えた。
- **原因**：`PhotoViewer` は `RecordRow`（札）の中で描いている。見た目は `position: fixed` で全面を覆っていても、**DOM では札の子孫**。`useSwipePages`（Today の日・週・月送り）は面ぜんたいで **touch** を受けているため、窓の中の払いがそこまで上がって届いた。窓は **pointer だけ** `stopPropagation` していて、touch は止めていなかった。
- **いまの守り（二重）**
  1. `PhotoViewer` の外わく（`rootRef`）に、touch / mouse / wheel / pointer の `stopPropagation` を直に付ける。`touchmove` / `wheel` は既定の動き（うしろの巻き物）も止める（ボタンの上から始まった指は止めない。click が届かなくなるため）。
  2. `useSwipePages` は、`.ft-sheet-wrap` / `[data-ft-overlay]` の中で始まった指を送りに数えない。**札の中に作った紙（「別のリストへ移す」など）も、同じ理由でこれで守られる。**
- ❌ **窓・紙の指の受け取りを pointer だけにしない。** touch で待っている仕掛け（`useSwipePages` など）が、祖先にいる。
- ❌ **`useLockBackground`（body を留める）だけで「うしろが動かない」と思わない。** 留めるのは巻き物で、指の行き先ではない。
- 閉じたら自動で解ける（窓を消せば、`useLockBackground` の後始末と、外わくごと消える見張りが一緒に外れる）。
- 確かめ方：Today で絵つきのメモを開き、拡大して横に払う → 次の絵へ移るだけで、日付は動かず、窓は消えない。縦に払って閉じたあとは、Today を左右に払って日が送れること。絵の上でうしろの一覧を送ろうとしても動かないこと。

## 6-⑩　拡大窓の二度たたき／二度クリック（2.17.2〜）

- 二度たたき（マウスなら二度クリック）で拡大、もういちどでもとの大きさ。`PhotoViewer` の `onTap` が受ける。
- 直したこと：①拡大中は指を置いた時点で「ずらす操作（pan）」になり、離しても「たたき」に届かず、**二度たたきでもとに戻せなかった**（pan で動かさず離したときも `onTap` へ）。②待つ長さを、指は 280ms、マウスは 450ms にした（二度クリックは間があく。280ms では二度目の前に閉じてしまっていた）。
- 拡大中の**一度たたきでは閉じない**。閉じるのは、もとの大きさのときだけ。
- マウスの右ボタンは、たたきに数えない。
- ❌ ブラウザの `dblclick` を足さない。pointer 側の二度たたきと二重に働き、拡大してすぐ戻る。
- 確かめ方：絵を開き、二度クリック（二度たたき）で拡大 → もういちどでもとの大きさ。拡大中に一度だけクリックしても閉じない。もとの大きさで一度クリックすると、少し待って閉じる。

## 6-⑪　紙の中の字を払っても、紙は動かさない（2.17.3〜）　⚠️ 見張りを「入力中だけ」に戻さないこと

- **起きていたこと**：「計画の設定」などの紙で、見出し・ラベル・入力欄の中・ボタンの字を指で上へ払うと、**紙ぜんたいが上に動いたり、うしろの一覧が送られたり**した（`useLockBackground` が引き戻すので、揺れて戻る動きにもなった）。
- **原因**：iOS は、紙の中に送れる余地が無くても、指の動きでページ（見える窓）を送ってしまう。2.16.7 で見張り（`installKeyboardInset` の `tgMove`）を入れたが、二つの穴があった。
  1. **「紙で打っているあいだ」だけ**有効だった（`setCtx` が `setGuard(c === "sheet")` を呼ぶ形）。キーボードを出していないときは素通しだった。
  2. **`input, textarea` を明示的に除外**していた（「カーソルを動かすため」）。テキストフィールドの上の払いが、そのまま紙を動かしていた。
- **もうひとつの原因（計画名・フォルダ名から画面が持ち上がる件）**：紙は見た目こそ画面いっぱいの `fixed` だが、**DOM ではその紙を開いた画面の子孫**（`PlanSettingsSheet` は `PlanDashboard` の中で描いている）。iOS は入力欄から「送る箱」を探すとき DOM をさかのぼるので、紙を通り越して**うしろの画面の送り場やページ**まで動かす。見張りが「親に送る余地があれば素通し」だと、ここが素通しになっていた。
- **いまの守り**
  - 指が `.ft-sheet-wrap` の上に降りた瞬間（`touchstart`）に `touchmove` の見張りを付け、離したら（`touchend` / `touchcancel`）外す。`touchstart` は `touchmove` より先に届くので、これで間に合う。
  - **字を打つところ（`input` / `textarea` / `contenteditable`）から始まった指は、親をさかのぼらず、その場で止める。** 1行の入力欄は中を縦に送る必要がない。`textarea` だけは**自分の中に送る余地があるときだけ**送らせる。
  - それ以外の指は、これまでどおり親をさかのぼって、紙の中で**実際に送れる箱**（タグの一覧・アイコンの一覧・横に並ぶ絵など）があれば送らせる（`canScroll`）。無ければ `preventDefault`。
  - **止めたうえで、さらに引き戻す（`tgPin`）。** ①紙の外わく自身の `scrollTop` を 0 へ、②紙の**外がわ**にある送り場（指を置いた時点の位置を `tgOuters` で控えてある）を元へ、③ページの送り位置を元へ。`e.cancelable` が false で止められないとき（送りがもう始まっている指）も、引き戻しだけは行う。指を離したあとも惰性で送られるので、数こま（`requestAnimationFrame` 8回）見張る。
  - 指が2本以上のときは見張らない（写真をつまんで大きさを変える操作を殺さないため）。
- ❌ **`passive: false` の `touchmove` を、いつも付けておかないこと。** アプリぜんたいの送りが重くなる。付けるのは指が紙に降りたときだけ。
- ❌ **見張りを `setCtx`（キーボードの文脈）に戻さないこと。** 上の①がそのまま再発する。
- ❌ **「親に送る余地があれば素通し」に戻さないこと。** 紙の親はうしろの画面なので、それは「うしろの画面を動かしてよい」と言っているのと同じ。
- ❌ **`useLockBackground` の引き戻し（`scroll` を聞いてから戻す）だけに任せないこと。** 指を置いたまま送っているあいだ、iOS は `scroll` のあとの `scrollTo` をすぐ打ち消すので、押さえている間じゅう画面が持ち上がったままに見える。`touchmove` のたびに戻すこと。
- ❌ **全画面（`[data-ft-overlay]`、記録の入力など）には広げないこと。** 欄が伸び、ほかの欄を見ながら書くので、送れるままにしてある。
- **副作用（承知のうえ）**：入力欄を**なぞってカーソルを動かす**（iOS の既定の操作）は効かなくなる。カーソルは、置きたいところを軽く叩けば動く。戻したいときは `canScroll` の `TEXTAREA` の扱いを `INPUT` にも広げるのではなく、`tgMove` の入口で `input` を除外する形に戻すこと（＝上の②に戻る、という判断になる）。
- 確かめ方：実機（iPhone）で「計画の設定」を開き、**キーボードを出したまま**、①計画名の欄の中、②タイトルの字、③「アイコン」のラベル、④足もとの「保存」の字、を順に上へ払う → **紙も、うしろの計画一覧も、いっさい動かないこと**（うしろの見出しがステータスバーに食い込まないこと）。フォルダ名の小窓（`NameDialog`）でも同じ。アイコンの一覧は、これまでどおり送れること。写真を2本指でつまんで大きさが変えられること。

## 6-⑫　灰の下地（2.19.0 で白に戻した）・折りたたみ・イベントのタブ（2.18.0〜）

**① 計画の画面の下地は白（`.bg-app`）── 2.18.0〜2.18.3 の灰は、2.19.0 で白に戻した**
- 2.18.0 で計画の画面（`PlanDashboard`）の下地を `#F2F3F5`（`.bg-canvas`）にしたが、**画面ぜんたいが暗く沈んで見えた**（持ち主の指摘）ため、ほかの画面と同じ白（`bg-app`）に戻した。札の境目は、札そのものの細いふち（`card-soft`）で足りる。
- ❌ **計画の画面の下地を灰（`bg-canvas`）に戻さないこと。** 灰の下地の上に、灰のタブのレール（`on-canvas`）まで置いたことで、いっそう重く見えた。
- `.bg-canvas`（app.css）・`--color-canvas`・`PillTabs` の `onCanvas`・`.ft-pill-track.on-canvas` は、いまどこからも使っていない。**消さずに残してある**（また灰の下地を使う画面を作るときのため）。使うときは、その画面の札がすべて白か、暗く沈んで見えないかを実機で確かめてから。

**② 折りたたみ（`Collapse`）**
- 開け閉めを、高さと薄さの `transition`（260ms、`--ease-in-out`）でなめらかにする。長さは `FT_COLLAPSE_MS`（app.js）＝ `--ft-collapse`（GLOBAL_CSS）で**必ず同じ**。
- 使っている所：記録を書く画面の日時（`WhenCollapse`）／Today の「他◯件を表示」（`PlanDueCard`）／探す画面の条件の欄／フォルダ「手動で入れる」の条件の欄。
- `keepMounted`：閉じても中身を残す（`display: none` にするだけ）。**探す・フォルダの条件の欄は外さないこと**（以前の `hidden` と同じく、打った条件を残す）。既定は「閉じ終わったら描かない」（`WhenCollapse` の決まりのまま）。
- 決まり：
  - ❌ **動き終わったあとに `overflow: hidden` / `opacity` を残さないこと。** 1 未満の `opacity` は重なりの層を作り、中から開く紙（日付えらびなど）が下に潜る。`Collapse` は動いている間だけ style 属性に入れ、終わったら外している。
  - ❌ **`transform` で動かさないこと。** 中の `position: fixed`（日付の紙）がこの箱を基準にしてしまう。
  - ❌ **`.ft-collapse` の `display: flow-root` を外さないこと。** 外すと中身の最初と最後の余白（`mt-*` / `mb-*`）が箱の外へ漏れ、伸びきった瞬間にその余白ぶん高さが変わって最後にカクッと跳ねる。中身の上の余白は、なるべく `pt-*` で取る（`WhenCollapse` は `pt-2`）。
  - 途中で押し返したら、そのときの高さから折り返す（跳ねない）。「画面の動き」オフ／視差効果を減らすでは、動かさずその場で切り替える。
- 確かめ方：各折りたたみを開け閉めし、押した瞬間に下の中身がガタッと動かず、なめらかに伸び縮みすること。開きかけで押し返して跳ねないこと。日時の欄をひらいて日付の紙が前面に出ること。視差効果を減らすでは、その場で開くこと。

**③ イベントの「進行中／完了済」タブ**
- 計画の画面のイベントを、`PillTabs` のタブで出し分ける。以前は済んだぶんを下の「やり終えた◯件」にたたんでいた（この折りたたみは無くなった）。件数はタブの名前の横に出す。
- はじめは「進行中」。**ぜんぶ済んだ計画だけ「完了済」から開く**（空の「進行中」を見せない）。
- 済んだイベントは、進行中と同じ札の形（`StepCard` の `inset` は使わない）。固定のボタンは出さない（以前と同じ）。
- **左右に払って切り替えられる（2.19.0〜、持ち主の指示による。以前の「付けないこと」は取り下げた）。**
  - 左へ払う＝「完了済」、右へ払う＝「進行中」。**端で輪にしない**（「進行中」で右へ払っても何も起きない）。計画の一覧（`PlanScreen`）は3つのタブを輪にしているが、こちらは戻る払いと向きが同じなので、輪にすると戻るつもりでタブが飛ぶ。
  - 受けるのは `useSwipePages`。中身の巻き物（`flex-1 overflow-y-auto`）に `areaRef` を付けてあり、**イベントの札の上でも、記録の上でも**払える。
  - ❌ **`useSwipePages` に `{ edgeGuard: 48 }` を渡すのをやめないこと。** 左端の透明な帯（`useEdgeSwipeBack`、幅 36px）で始まる払いは「戻る」。その少し内側（36〜48px）で戻るつもりの指が始まっても、タブが切り替わらないようにしている。
  - ❌ **`useSwipePages` の「重なった窓・紙の上で始まった指は数えない」判定を、`closest(...)` だけに戻さないこと。** 計画の画面そのものが `OverlayScreen`（`data-ft-overlay`）の中にあるので、いちばん近い `data-ft-overlay` は自分の外わくになり、面のどこを払っても届かなかった（この判定のままでは、`PlanDashboard` に `useSwipePages` をつないでも何も起きない）。いまは「**この面の子孫**の窓・紙」だけを除く（`el.contains(ov)`）。
- ❌ **切り替えに動きを付けないこと**（同じ階層の切り替えは動かさない）。`useSwipePages` が返す `pageCls`（横に揺れる動き）は使わない。
- 計画の一覧（`PlanScreen`）のタブも同じ `PillTabs` に置き換えた（見た目は同じ）。

## 6-⑬　記録カードは、タップで色を付けず、長押しのときだけ付ける（2.18.1〜、2.18.2 で色の付き方を調整）　⚠️ `ft-hold` を外さないこと

- **起きていたこと**：記録カードを**タップしただけで**、札に色（暗い膜）が付いた。ふつうのタップでも「何か始まった」ように見えた。
- **原因（2つ）**
  1. CSS の `.ft-tap-card:active::after` が、指が触れた瞬間に膜を出していた（タップでも長押しでも同じ）。
  2. JS（`RecordRow` / `StepCard`）が、`pointerdown` の**その場で** `.ft-pressing` を付けていた。長押しが決まる前から色が付いていた。
- **いまの形**
  - 札に `ft-hold` を付ける。`.ft-tap-card.ft-hold:active::after` は `opacity: 0`（タップでは色を付けない）。
  - JS は、押さえてから `FT_HOLD_TINT_MS`（150ms）たってもまだ押さえていたときだけ `.ft-pressing` を付ける。**付いたら、その場で最大の濃さ（`.045`）に切り替える（2.18.2〜）。** CSS の `transition-duration` は 40ms（他の札の「沈むのは速く」と同じ考え方）。決まったら外す。
  - 付けている札：`RecordRow`（ふだんの `article` と、選ぶ形のときの外わく）／`StepCard`（イベントの札。RecordRow と同じ手ざわりにそろえる決まりのため）。
  - 付けていない札：計画・フォルダの札。押すと画面がひらく札なので、押した瞬間の色は「押した」の返事としてそのまま残してある。
- 決まり
  - ❌ **`pointerdown` のその場で `setPressing(true)` を呼ばないこと。** 1 の直しだけでは、タップでも一瞬色が付く。
  - ❌ **`.ft-pressing` の色を、じわじわ濃くする動きに戻さないこと（2.18.1 の作り）。** 持ち主の指示により、色は「初めから濃くてよい」に変更した（2.18.2）。長押しの合図は、色そのものではなく「付くまでの待ち」（`FT_HOLD_TINT_MS`）で出す。
  - `.ft-pressing` 側のセレクタは `:active` 付きも並べて書いてある（`.ft-hold:active` の `opacity: 0` より強くするため）。消さないこと。
- 確かめ方：記録カードとイベントの札を、①すばやくタップ → 色が付かない、②押さえつづける → 少し間があってから、いきなり最大の濃さで色が付き（だんだん濃くはならない）、そのまま選ぶ形（イベントは設定の紙）に入る。中のチェック・固定・編集のボタンを押したときは、札ぜんたいに色が付かない（これまでどおり）。

## 6-⑭　2.19.0 のまとめ（計画の画面の灰をやめる・払いでタブ切り替え・横長の上そろえ）

- 計画の画面の下地：灰 → 白（6-⑫ ①）。
- 計画の画面の「進行中／完了済」：左右に払って切り替え（6-⑫ ③）。`useSwipePages` に `opts.edgeGuard` を足し、重なる画面の中でも使えるよう判定を直した。
- 計画の一覧（横長）：段組み → 格子（3章「横長の画面で、2段の札の上がそろわない」の末尾）。
- 版数は、混ざった中でいちばん大きい変更（払いの追加＝Y）に合わせて 2.18.3 → **2.19.0**。

## 7. 直ったかどうかの確かめ方

`tests/` に、`window.storage` へ 20〜400ms のばらつく遅延を持たせた再現テストがあります。
Node があれば動きます（ブラウザは要りません）。

```
cd tests
node run.js storage-current.js        # 修正前の保存層。2項目が不合格になる（再現）
node run.js storage-patched.js fixed  # 修正後。全項目合格
node run-tap.js                       # タップの受け口と、読み返しの歯止め
node run-photo.js ../app.js           # ヘッダー・アイコンの絵が開き直しで残るか（修正前の app.js を渡すと4項目が不合格）
```

- `run.js` … 連続タップ／保存の割り込み／専用ストレージだけ書けなかったとき／`window.storage` があとから生える場合／でたらめな間隔で40回タップ
- `run-tap.js` … `click` が配られない状況で、押したぶんがすべて届くか
- `run-photo.js` … 古い本体の片づけ／専用ストレージにだけ入らず控えが消えた／置き場が返事をしない／失われた絵があるときの控え作り（`app.js` から直接切り出して動かす）

保存層を直したら `storage-patched.js` を取り直すこと（`app.js` から `const LOCAL_NEWER` ～ `const REC_KEY` の手前までを切り出したもの）。

---

## 8. 実機で、また戻ったときの切り分け

1. Mac の Safari の「開発」メニューから iPhone のコンソールを見る。
2. 「保存を読み返したら食い違っていたので、書き直します」または「控えにしか入っていなかったので、専用ストレージへ入れ直します」が**出ている** → ①の置き場で取りこぼしている。
   ヘッダー・アイコンだけ消えるときは「ヘッダー・アイコンの絵がどこにも残っていません」が出ていないかも見る（⑤）。
3. **出ていない**のに戻る → ②の状態か③のタップ。
   - ゆっくり押せば平気で、速く押したときだけ戻る → ③
   - どちらの速さでも戻る → ②（どこかで render スコープから丸ごと写している）
4. そもそも直した覚えのある挙動が何も変わっていない → ④の版数を疑う。

**画面の動きがおかしいとき（2.11.0〜）**

- 画面が出るたびに動かない → 「画面の動き」がオフか、端末の「視差効果を減らす」がオンになっていないか。次に `GLOBAL_CSS` の `anim-*` が空に戻されていないか。
- 開いたあと、しばらくしてもう一度スライドする → 誰かが「動き終わったらクラスを外す」形に書き換えていないか（3章）。
- 開いた直後だけ、下のボタンやドラムの紙の位置がずれる → `anim-*` に `forwards` / `both` が付いていないか。
- **重なる画面を開くと、下タブ・暗がり・右下の＋がまとめて上へ持ち上がる（2.11.16 で修正）** → `useLockBackground` で `body` に `position: fixed` を当てていないか。body を流れから外すとページが送れなくなり、iPhone は `viewport-fit=cover` で広げていた画面を測り直す。下端が 60px 前後切り上がり、`fixed` のものが全部それに追従する（実機の画面写しでは、下タブ・暗がり・右から出た紙が**すべて同じ高さで切れていた**＝フッター単体の不具合ではない見分け方）。うしろを留めるのは `overflow: hidden` ＋「送られたら引き戻す」で足りる。app.css の末尾にも `body { position: static !important }` の用心を置いてある。
- 下へ送ると、見出しや帯がその場で透けて消える → どこかで sticky に戻していないか（3章「画面の上に留める見出しと帯」）。
- 開いた画面の中だけ、縦に送った感じが重い → 箱に `data-ft-entered` が付いているか、Safari の要素の検証で見る。
- 下タブをもう一度押しても上へ戻らない／1回めでいきなりデフォルトへ飛ぶ → `BottomNav` の `onChange` で `k === tab` のときだけ `tabReset` を増やしているか、画面に `resetSig` を渡しているか、`viewKey` に表示を変えるもの（タブ・検索語）が入っているか（3章「下タブをもう一度押したときの戻り方」）。
- メニューから移るとき、メニューが滑って新しい画面とすれ違う → `goFromMenu` で `setMenuInstant(false)` をタイマーに入れ直していないか。

---

## 9. さらに詳しく知りたいとき

`UIデザイン仕様書_ドラフト.md` に、これまでの全ての調査・修正の経緯（何を調べて、何が問題で、どう直したか、どう検証したか）を章立てで残しています。「なぜこのルールになったのか」「この値の根拠は何か」を確認したいときはそちらを参照してください。
