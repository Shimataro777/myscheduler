# Footprints 仕様書

聖書・教会の記録アプリ「Footprints」の、画面ごとの細かい動きをまとめたものです。
「こう操作したら、こうなる」を確かめるための資料としてお使いください。

数値はすべて実際のコードから拾っています。

---

## 1. 記録の種類と項目

記録は4種類あります。（種類の名前を変える設定は取りやめました。すでに変えていた名前は、そのまま残ります）

| 種類 | 入力する項目（上から順） |
|---|---|
| **通読** | 日付／読んだ箇所／メモ／タグ |
| **学び** | 日付／テーマ／聖書箇所／主題聖句／メモ／タグ |
| **聖句** | 日付／聖書のことば／メモ／今月の聖句／今年の聖句／タグ |
| **その他** | 日付／メモ／タグ |

- **閲覧画面の並びは、入力画面と同じ順**です。
- 「テーマ」には聖書箇所の挿入ボタンを付けていません。題を書く場所のためです。
- 一覧に出る見出しは、学びなら「テーマ→聖書箇所→主題聖句」の順に、最初に見つかったものを使います。

### 記録の書きかけ

- 入力中は自動で下書きが残ります。アプリを閉じても、次に開いたときに続きから書けます。
- 「途中保存」のボタンはありません（2.0.0〜）。入力が止まって0.8秒後に自動で残ります。
- 左上の×の反対側に、元に戻す・やり直す、ピン留め、ブックマークがあります（同じ欄への続けた入力はひとまとめで戻ります）。
- 保存は、中身が空のあいだは押せません。
- 書きかけのまま閉じようとすると「保存する／保存しない／キャンセル」を尋ねます。

---

## 2. 日付

- **カレンダーから選びます。** 曜日の見出し（日〜土）が出て、日曜は赤、土曜は青です。
- 今日の日付は枠で示され、選んだ日はテーマ色で塗られます。
- 年月を押すと、年と月をまとめて選べるドラムが下から出ます。
  年は**今年をまんなかに前後100年ぶん**（1926〜2126）並びます。
- 「今日」ボタンで今月に戻ります。
- 対象は「記録の日付」と「探すの期間（開始・終了）」の3か所です。

---

## 3. タグ

- ひとつの記録に何個でも付けられます。上限は **1つのタグにつき24文字**。
- 付けるときは、ボタンを押してダイアログから選びます。画面にタグ一覧を出しっぱなしにはしません。
- **一度作ったタグは一覧に残ります。** その記録を消しても、次から選べます。
- 打ち込むと絞り込み検索ができます。まだ無い言葉のときだけ「作る」が押せます（同じタグの重複を防ぐため）。
- **並び順は自由に変えられます。** 「タグの整理」で右端の三本線をつまんで上下に動かします。
  決めた並びは保存され、記録画面のタグ選びにも同じ順で出ます。
- メニューの「タグの整理」で、追加・名前の変更・削除ができます。
  - **名前を変えると、そのタグが付いた記録もまとめて変わります。**
  - **削除すると記録からタグが外れますが、記録そのものは消えません。** 何件から外れるか事前に知らせます。

---

## 4. 聖書箇所の認識と色付け

### 認識される書き方

- 「創世記 1章」「ヨハネの福音書 3:16」「マタイの福音書 2章-5章」など。
- 章の範囲（2章-5章）は、間の章もすべて読んだものとして数えます。
- 書名は略称・別名でも認識し、正式名に直して扱います（例：「ヨハネの第一の手紙」→「ヨハネの手紙 第一」）。

### 引用ブロック（閲覧画面）

本文と聖書箇所が続いているとき、**左にテーマ色の縦線を引いた引用ブロック**として表示します。

引用と見なすのは次の2つの書き方です。

1. 括弧に入れる … `本文（ヨハネの福音書 3:16）` ※全角・半角どちらでも
2. 行の終わりに置く … 本文のあとで改行し、その行に `ヨハネの第一の手紙 2:27`

**文の途中に出てくる箇所は引用にしません**（「ヨハネ 3:16 について考えた」など）。引用でないものまで囲ってしまうためです。

- 引用の範囲は、直前の空行の次から、聖書箇所の行の終わりまでです。
- 引用の前後に空けた空行は、そのぶんの余白として再現されます。
- 縦線で囲まれた範囲が、引用ブロックです。
- 聖書箇所そのものに色や太字は付けません（引用は縦線で示しているため）。

---

### 短い略称の扱い

「創」「出」「民」「申」「詩」のような**1〜2文字の略称**は、ふつうの日本語の中にも現れます。
そのまま拾うと「国民 3割」が『民数記 3章』になってしまうため、短い略称のときだけ次を確かめます。

- 直前が漢字・英数字でないこと（「国民」「提出」のような語の一部を弾く）
- **1文字の略称は「章」か「:」が付いていること**（「出3人」を弾く）

正式名（「創世記」など）は、これまでどおりどこに書いても認識します。

---

## 4-2. この箇所には過去のメモがあります

記録の入力画面で、いま扱っている箇所を過去にも書いていたら、
たたんだ札でそっとお知らせします。押すと中身が開きます。

### どの画面に出るか

| 種類 | 出る／出ない | 手がかりにする欄 |
|---|---|---|
| **通読** | 出る | 「読んだ箇所」（選んだ書・章。選んでいなければ、手で書いた文字から読み取る） |
| **学び** | 出る | 「聖書箇所」 |
| 聖句 | 出ない | — |
| その他 | 出ない | — |

- 札の位置は、箇所の欄のすぐ下です。
- **手がかりにするのは「箇所」の欄だけ**です。メモ欄は見ません。書いている途中で
  文が揺れ動くたびに札が出入りすると、落ち着かないためです。
- 箇所が空のあいだは、札そのものが出ません。

### 何を並べるか

- **すべての種類の記録**（通読・学び・聖句・その他）から探します。
- いま書いている記録そのものは除きます。中身が空の記録も除きます。
- **日付の新しい順**に、**最大6件**まで。
- 1件ずつ、種類の札・日付・本文の頭4行を見せます。

### 「同じ箇所」の決めかた

**章だけでなく、節まで見て決めます。**

| いま入れた箇所 | 過去の記録 | 出るか |
|---|---|---|
| ヨハネの福音書 3:13-24 | ヨハネの福音書 3:13-24 | 出る |
| ヨハネの福音書 3:13-24 | ヨハネの福音書 3:20（範囲の中） | 出る |
| ヨハネの福音書 3:13-24 | ヨハネの福音書 3章（通読） | 出る（章ぜんたいは範囲を含むため） |
| ヨハネの福音書 3:13-24 | **ヨハネの福音書 3:1-12** | **出ない** |
| ヨハネの福音書 3:13-24 | ヨハネの福音書 1:1 | 出ない |
| ヨハネの福音書 3:13-24 | ルカの福音書 3:13-24 | 出ない |

- 節の範囲どうしは、**重なりが1節でもあれば**同じ箇所とみなします。
- どちらか一方に節が書かれていないときは、その章ぜんたいを指しているものとして扱います。

### 相手の記録が「どこを扱っているか」の決めかた

相手の記録は、**本文ぜんたい**（テーマ・箇所・主題聖句・メモ・タグ）から箇所を拾います。
メモの中に書いた箇所でも見つかるようにするためです。ただし次の2つを守ります。

- **章だけの言い及びは、同じ章に節つきの箇所があるときは数えません。**
  聖書箇所が「ヨハネの福音書 3:1-12」の学びで、メモに「ヨハネ3章の前半について」と
  書いてあっても、その記録が扱っているのは 3:1-12 です。
  ここを数えていたため、「3:13-24」を書いているときに 3:1-12 の記録が並んでいました。
- **通読で選んだ書・章は、いつも章ぜんたいとして数えます。**
  「その章を読んだ」というはっきりした事実なので、メモに節つきの引用があっても狭めません。

| 相手の記録 | 扱っている箇所 |
|---|---|
| 学び：箇所「3:1-12」 | 3:1-12 |
| 学び：箇所「3:1-12」＋メモに「ヨハネ3章の前半」 | 3:1-12（章だけの言い及びは数えない） |
| 学び：箇所「3:1-12」＋メモに「（ヨハネ 3:16）」 | 3:1-12 と 3:16 の両方 |
| 学び：箇所「3章」 | 3章ぜんたい |
| 通読：3章を選択＋メモに「（ヨハネ 3:5）」 | 3章ぜんたい |
| その他：メモに「ヨハネ3章」だけ | 3章ぜんたい |

### 閲覧画面の札との違い

閲覧画面の下に出る「この箇所を含む記録があります」も、**同じ決めかた**で選びます。
以前はここだけ「書と章」で見ていたため、3:13-24 を開いているのに
重なっていない 3:1-12 の記録が並んでいました。

| | 入力画面「過去のメモ」 | 閲覧画面「この箇所を含む記録」 |
|---|---|---|
| 見る細かさ | 節まで | 節まで |
| 手がかり | 箇所の欄だけ | 記録ぜんたい（章だけの言い及びは落とす） |
| 件数 | 6件 | 20件 |
| 押したとき | 開いて中身を読むだけ | ちょっと見る小窓 → その記録へ移れる |

---

## 5. 今月・今年の聖句

- 聖句の記録で「今月の聖句にする」「今年の聖句にする」を選ぶと、ホーム画面に出ます。
- **すでに別の記録が同じ月・年に登録されているとき**は、その記録の中身を見せて確認します。
  - 「変える」を選ぶと、こちらに付き、元の記録からは外れます。
  - **外れるのは「保存」を押したときだけ**です。確認後にキャンセルすれば、元の記録はそのままです。
- 閲覧画面の下部に「2026年7月の聖句」のような印が出ます。

### ホーム画面での表示

- 「今月／今年」の切り替えで、**1枚だけ**表示します。はじめは「今月」です。
- 切り替えは、両方登録があるときだけ出ます（片方だけなら選ぶ意味がないため）。
- 押した側から中身がすっと入ってきます（0.26秒）。
- たたんでいるときは2行まで。押すと全文が開きます。

---

## 6. 果樹（ホーム画面）

「種を選んで植える」から始めます。**通読した日数と記録の件数の両方**が条件に届くと次の段階へ進みます。

| 段階 | 姿 | 必要な日数 | 必要な件数 |
|---|---|---|---|
| 1 | ふかふかの土 | 0 | 0 |
| 2 | ちいさな芽 | 1 | 1 |
| 3 | かわいい双葉 | 3 | 3 |
| 4 | 本葉と小枝 | 5 | 6 |
| 5 | 青々とした若木 | 7 | 10 |
| 6 | 小さなつぼみ | 14 | 17 |
| 7 | 可憐な花（満開） | 21 | 25 |
| 8 | 青くて小さな実 | 27 | 32 |
| 9 | 大きく膨らんだ実 | 32 | 38 |
| 10 | 熟した美味しそうな実 | 35 | 42 |

- 日数・件数は**記録の種類を問わず**すべて数えます。
- 段階ごとにみことばが添えられます。
- 最後の段階になると押して収穫でき、収穫した実はメニューから振り返れます。
- **日数だけ経っても実りません。** 件数も必要です。

---

## 7. 実績

- 66巻それぞれについて、**初回既読の章数**と記録の件数を表示します。
- 読み終えた巻には印が付きます。
- 「日ごとの記録」で日付を押すと、その日の記録の一覧が別画面で開きます。
- 記録のある日は、日付の下に短い線で示します（枠では囲みません）。

---

## 8. 探す

- **キーワード**は、本文・タグ・聖書箇所のすべてを対象に探します。
- **絞り込み**は、記録の種類（複数選択可）／タグ（複数選択時はすべて含む）／書／期間。
- 検索ボタンは、**手がかりが何かひとつでもあり、かつ前回から条件が変わったとき**だけ押せます。
- 並べ替え（目次順・新しい順・古い順）は、**検索したあとにだけ**出ます。
  目次順は、記録の種類にかかわらず**本文に書かれた聖書箇所**で並べます
  （箇所を書いていない記録はいちばん後ろ）。
  **はじめの並び順は「画面のカスタマイズ」で決められます**（探す・書ごとの記録・ブックマークに反映）。
- 探しているあいだは、画面の中央にぐるぐるが出ます。

---

## 9. 記録画面

- いちばん上に「ここから始めましょう／続きから」。最後に読んだ箇所の次を示します。
- 「最近の記録」は**直近10件**。並び順は**作成日ではなく、最後に手を入れた日**です。
  - 古い記録を書き直すと、先頭に上がってきます。

---

## 10. バックアップと復元

### 書き出し

「データを保存」で出るのは **`Footprints-backup-2026-07-30.txt` の1つだけ**です。

このファイルは**そのまま読めて、復元にも使えます**。
前半が読みやすい形の本文、後半（目印より下）が復元用のデータです。

- 読みたいときは、メモ帳などで開けばそのまま読めます
- 戻したいときは、そのまま「データ復元」で選びます
- **目印より下は消さないでください。** 消すと復元できなくなります

### 文字でのやりとり（ファイルを使わない方法）

ファイルの保存先が分かりにくい端末のために、**文字だけでやりとりする道すじ**も用意しています。

| したいこと | 場所 |
|---|---|
| バックアップを文字でコピー | バックアップ画面「文字でコピー」 |
| その文字から復元 | バックアップ画面「文字から復元」 |
| 記録1件を文字でコピー | 閲覧画面の右上のコピーボタン |
| その文字から取り込む | 「何を記録しますか」→「文字から取り込む」 |

- コピーした文字は、メモ帳やチャットなど**あとで開ける場所に貼って残してください**
- 貼り付けの窓には「貼り付け先から読み取る」ボタンもあります（端末が許す場合）
- **文字でコピーした時点で「書き出した」として数えます**（うながしが消えます）。
  ただし貼り忘れると残らないので、貼って残すようお伝えする文が出ます

取り込むときは、**ファイルの名前や種類が変わっていても読み込めます**
（Androidで受け取ると名前が変わることがあるため）。
Footprintsのものでないファイルを選んだときは、そのことをお知らせします。

### 入っているもの

記録／イラスト／果樹／タグの一覧に加えて、**画面の設定も入ります**（テーマ色・文字の大きさ・イラスト表示・動きの演出・並び順・ひとこと・**ヘッダーの背景**・記録の種類の名前）。

「最後に書き出した日」だけは入りません。端末ごとの記録であり、持ち運ぶものではないためです。

### 復元

- **いまある記録は消えません。** 同じIDの記録は置き換わり、無いものは追加されます。
- タグの一覧は足し合わせます。
- 設定が入っていない古いファイルを読み込んだときは、**いまの設定をそのまま残します**。
- **復元した直後は「まだ書き出していない記録が…」のうながしは出ません。** 読み込んだデータはすでに書き出し済みのものだからです。

### 書き出しのうながし

次のどちらかに当てはまると、そっとお知らせします。

- 前回の書き出しから **14日**以上経った
- 書き出していない記録が **10件**以上たまった

---

## 11. 記録を1件だけ受け渡す

Footprintsを使う人どうしで記録を分け合えます。

- **送る側** … 閲覧画面の右上のボタンから。文字でコピーすることもできます。
  ファイルで渡すときは `Footprints-record-2026-07-30.txt` として共有します。
  （末尾が `.txt` なのは、Androidが受け取れるようにするためです。中身はこれまでと同じです）
  - ID・ピン留め・ブックマークは持ち出しません（その人の目印のため）。
- **受け取る側** … 「何を記録しますか」の一番下「ファイルから取り込む」から選びます。
  - **いまある記録は消えません。** 足すだけです。
  - IDは必ず振り直します（送り手のIDが偶然かぶっても上書きしません）。
  - 中身がそっくり同じものは足しません（同じファイルを二度取り込んだとき用）。
  - 受け取った記録に付いていたタグは、タグ一覧にも控えます。
  - 全件のバックアップファイルからも取り込めます。

---

## 12. イラスト（画面のカスタマイズ）

登録できるのは**全部で5枚**までです。

### 複数枚登録したときの割り当て

イラストは「まとまり」ごとに登録し、**そのまとまりが受け持つ場所へ、登録した順に割り当てられます。**

「通読」のまとまりは2か所を受け持っています（①通読の記録画面 ②実績の最後）。

| 登録枚数 | 通読の記録画面 | 実績の最後 |
|---|---|---|
| 1枚 | 1枚目 | 1枚目（同じ絵） |
| 2枚 | 1枚目 | 2枚目 |

- **登録できるのは、そのまとまりが受け持つ場所の数までです。** そろうと「ここに絵を追加」は押せなくなります（差し替えるときは、いらない絵を消してから足します）。
- **どの絵がどこに出るかは色で分かります。** 絵の枠と、その絵が出る場所の枠が同じ色になります。
  絵が1枚だけのときは、どの場所にも同じ絵が出るので、色もすべて同じになります。
- 全体の上限は5枚です。
- まとめて選ぶときの枚数も、空いている場所の数に合わせます。
  ただし**端末の写真選択そのものに「何枚まで」を伝える仕組みがない**ため、
  多く選ばれたぶんは受け取らずにお知らせします。
- 1枚も登録していないまとまりには、はじめから用意した絵が出ます。
- 「イラストを表示する」を切ると、すべての絵が消えます。

### ひとこと

- 記録画面のいちばん下、絵の下に中央そろえで出ます。
- **何行でも書けます。** 改行はそのまま表示されます。
- 初期表示は種類ごとに用意されています（聖句＝コロサイ人への手紙 3:16 など）。

---

## 12-1-2. テーマカラーが出てくるところ

選んだ色は、画面のあちこちに少しずつ現れます。

- 小見出し（「最近の記録」「3件の記録」など）
- 下のタブの選ばれている印と字
- 見出しの下の細い線
- ボタン・札・聖書箇所の丸い印・入力欄を選んだときの縁
- 文字をなぞって選んだときの色、入力中の縦棒

---

## 12-2. ヘッダーの背景

「画面のカスタマイズ」から、画面のいちばん上の帯に好きな写真を敷けます。

- 写真の上には**黒い膜**がかかり、見出しや三本線は**白い文字**になります。
  明るい写真でも暗い写真でも、文字が読めるようにするためです。
- 膜は下へ行くほど濃くなります。見出しが並ぶのは下のほうだからです。
- 自前の下地を持つボタンは、色がそのまま残ります。
- 写真を選ぶと、下から「帯にする場所を決める」が開きます（2.3.0〜）。
  - 窓は**いまのヘッダーの帯と同じ形**です。窓に見えている範囲が、そのまま帯に出ます。
  - 指で動かして位置を、2本指でつまんで大きさ（1〜4倍）を決めます。写真の外は写せません。外へ引いたり、つまみすぎたりすると少しだけ出て、指を離すと戻ります。
  - 「決定」で見本に入り、下の「保存」で反映されます。「キャンセル」や暗いところを押すと、何も変わりません。
  - 帯の形は文字の大きさや端末で変わります。あとから文字の大きさを変えたときは、選び直すと形がそろいます（そのままでも、まん中に寄せて出ます）。
- 選ぶ画面には見本が出ます。実際の見えかたと同じ膜がかかり、帯と同じ形をしています。
- イラストとは別枠で、1枚だけ持てます。「外す」で無地に戻せます。
- バックアップに含まれ、復元でも戻ります。

## 13. 文字の大きさ

- 「小・中・大」の3段階。初期値は「小」（これまでと同じ大きさ）です。
- 見出しは本文より控えめな倍率で大きくなります。同じ倍率だと画面の幅に収まらないためです。
- **入力欄の文字だけは変わりません（常に16px）。** 16pxより小さいと、iPhoneが入力時に画面を勝手に拡大してしまうためです。

---

## 14. 動きの演出

- 「画面のカスタマイズ」で止められます。止めると、押した感触以外の動きがすべて無くなります。
- 端末側で「視差効果を減らす」を設定している場合も、自動的に控えめになります。

---

## 14-2. 画面のつくり（下の帯と丸ボタン）

- 下の帯（ホーム／記録／探す／実績）は、画面のいちばん下に貼り付いています。
  **厚みはいつも同じ**で、文字の大きさを「大」にしても変わりません
  （ここが厚くなると、記録を見せる場所がそのぶん減るためです）。
- **右下の丸ボタンは、帯と重なりません。** 帯との間は約39px空きます。
  大きさは＋（記録画面）も鉛筆（閲覧画面）も同じ56px角です。
- 三本線メニューのいちばん下の段は、**下の帯と同じ高さ**です。
- いちばん下のホームバーのところは、下の帯と同じ白でつながります。

---

## 14-1-2. 起動したとき

アプリ名と双葉のアイコン、小さな版数が出る画面を**2秒ほど**はさんでから、
ふわっと消えてホーム画面が出ます。読み込みが速い端末でも、必ず2秒は出ます
（一瞬だけ光って消えると、かえって目ざわりなためです）。

---

## 14-3. 記録を失わないための仕組み

記録はこの端末の中だけにあります。失われにくくするため、次のことをしています。

- **起動したとき、端末に「この記録を消さないでほしい」と頼みます。**
  iPhoneでは、ホーム画面に追加していないと、しばらく使わないだけで
  消されることがあります。頼んでおくと、その見込みが下がります
- 三本線メニューの「バックアップ」の下に、**最後に書き出した日**が出ます。
  まだ控えていない書きかえがあるときは、その件数も出ます
- バックアップ画面を開くと、**まだ控えていない書きかえ**が上に出ます。
  端末の置き場の広さと、いま使っている量も分かります

---

## 15. リンク

- メモなどに書いたURLは、閲覧画面で押せるようになります（落ち着いた青・下線なし）。
- 押すと**iPhoneの確認が出て、そのままアプリ内ブラウザで開きます**（下からせり上がります）。
- YouTubeもそのまま再生できます。

---

## 16. 起動時

- 読み込みが終わるまで、アイコンとくるくるが出ます（白い画面にはなりません）。
- 終わるとふわっと消えてメイン画面へ切り替わります。
- うまく読み込めないときは、同じ画面に確認事項が出ます。

---

## 付録：確かめ方について

この仕様書に書いた動きは、自動で確かめる仕組み（全64種類）で毎回検証しています。
画面の見た目（余白・重なり・大きさ）も、実際に描画して数値で測っています。

開発を引き継ぐ際は、`HANDOFF.md` に「やってはいけないこと」と、
過去に実際つまずいた点をまとめてありますので、あわせてご覧ください。

---

## 付録2：2.0.0 で変わった操作

- **見出しと検索の帯は画面に固定**です。下へ送っても動きません。
- **下のタブをもう一度押す**と、1回めは一番上へ、2回めは「探す」の条件と結果を消します。
- **×・キャンセル・戻るは待たずに閉じます。** 保存も押した瞬間に行います。
- **日付は1行**に畳まれ（「今日・2026年9月19日（土）」）、押すとカレンダーが開きます。タグは一番下です。
- **聖書箇所を挿入**のボタンは、入力欄の枠の中（下の帯）にあり、読み取れた聖書箇所の札も同じ帯に並びます。押すと窓が開き、書・章・節（終わりの章・節も）をそれぞれドラムで選びます。
- **今月／今年の聖句**は、スイッチの2行です。
- **「何を記録しますか」**の各行の説明文はありません。
- **探す**の絞り込みは、項目名と「？」を外し、「期間」だけに名前があります。

## 付録3：2.1.0 で変わった操作

- **「〇〇を聖句に追加」は無くなりました。**
- **聖書箇所＋空行＋聖書箇所**の間隔は、書いた空行の数のとおりになります（以前は広く空いていました）。
- **探す**：当たった語に色が付きます。見えている文に語がないときは、その前後の文が札の下に出ます。
- **探す**の結果を**長押し**すると選ぶモードになります。下の帯で「すべて選ぶ／削除／完了」ができます。
- 並べ替えは、小さな文字のボタンを押すたびに「目次順 → 新しい順 → 古い順」と入れ替わります。

## 付録4：2.1.1

- 引用（左に縦線が付く聖書箇所）の間隔を詰めました。空行を書いていないときはごく狭く、空行を書いたときも、そのままの高さは空きません。

## 付録5：写真（2.2.0）

- どの種類の記録でも、いちばん下（タグの上）に写真を **4枚まで** 付けられます。
- 写真は縮めて保存されます（長辺900px）。記録そのものとは別の場所に置かれるので、記録の保存を圧迫しません。
- 写真は、枚数にかかわらず2列・正方形で並びます。追加のボタンには「あと○枚」と出ます。
- 記録の札には小さな見本が並び、閲覧画面では本文の下に出ます。押すと大きく見られます。横に払うと次の写真、下に払うと閉じます。
- 記録を消すと、その写真も消えます。
- バックアップには写真も一緒に保存され、読み込むと元どおりになります。
- ヘッダーの背景に選んだ画像も、同じ場所で持つようになりました。これまでに設定した画像はそのまま使えます。
